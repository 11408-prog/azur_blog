# 开发守则（博客维护指南）

> 目标读者：任何人 / 任何 AI 协作者。
> 看完本文档后，你应该能回答两个问题：**博客结构是什么**、**想改某个东西该改哪个文件**。

- 技术栈：**Hexo 8**（静态博客框架）+ **Butterfly** 主题
- 内容格式：Markdown，存放在 `source/_posts/`
- 构建产物：`public/`（自动生成，不要手动改，已在 `.gitignore` 中）

---

## 1. 常用命令

| 命令 | 作用 |
|---|---|
| `npm run build` | 生成静态站点到 `public/`（等价 `hexo generate`） |
| `npm run server` | 本地预览，访问 `http://localhost:4000` |
| `npm run clean` | 清理 `public/` 与 `db.json` 缓存（改配置后最好跑一次） |
| `npm run deploy` | 部署（**当前未配置**，见「已知问题」） |
| `npx hexo new "文章标题"` | 新建文章到 `source/_posts/` |

标准流程：`npm run clean && npm run build`，然后 `npm run server` 本地查看。

---

## 2. 目录结构（当前真实状态）

```
blog/
├── _config.yml                  ← 站点配置（标题、URL、主题、搜索）
├── package.json                 ← Node 依赖与 npm 脚本
├── package-lock.json            ← 依赖锁定（不要手动改）
├── node_modules/                ← 已安装依赖（git 忽略）
│
├── scaffolds/                   ← 新建文章/页面的模板
│   ├── post.md                  （文章模板）
│   ├── page.md                  （页面模板）
│   └── draft.md                 （草稿模板）
│
├── source/                      ← 所有「原料」都在这里
│   ├── _posts/                  ★ 文章（Markdown）
│   │   ├── 博客制作注意事项.md
│   │   ├── 常用指令.md
│   │   ├── 开发遇到的问题以及对策.md
│   │   └── 我的第一篇博客.md
│   ├── categories/index.md      ← 「分类」页面（Hexo 原生）
│   ├── tags/index.md            ← 「标签」页面（Hexo 原生）
│   ├── img/                     ← 图片资源
│   │   ├── avatar.png           （头像）
│   │   └── covers/              （页首封面图，当前用 enterprise_2.jpg）
│   ├── css/                     ← 自定义样式
│   │   ├── custom.css
│   │   └── welcome_animation.css
│   ├── js/                      ← 自定义脚本
│   │   ├── welcome_animation.js
│   │   ├── weather.js
│   │   └── carousel.js
│   └── _data/butterfly.yml      ← 主题备用配置（当前为空，暂不使用）
│
├── themes/butterfly/            ← 主题本体（vendor 进 git）
│   ├── _config.yml              ★★ 主题配置（最常改的文件之一）
│   ├── layout/                  ← Pug 页面模板（一般不动）
│   ├── source/                  ← 主题自带 CSS/JS/图片
│   └── scripts/                 ← 主题辅助脚本
│
├── public/                      ← 构建产物（自动生成，git 忽略）
├── db.json                      ← Hexo 缓存（git 忽略）
├── .gitignore
├── .github/                     ← CI 配置（当前为空）
├── .claude/                     ← Claude Code 配置 / 技能（git 忽略）
│   └── skills/kill-ai-slop/     ← 「去 AI 味」技能
└── 开发守则.md                   ← 本文档
```

> 注意：`source/技术/`、`source/热爱/` 等空目录已删除，**分类统一走 Hexo 原生机制**（文章 front matter 里写 `categories`，自动生成 `/categories/xxx/` 页面）。不要再在 `source/` 下手动建分类目录。

---

## 3. 想改什么 → 改哪个文件

| 你想做的事 | 改哪个文件 |
|---|---|
| 改站点标题 / 副标题 / 描述 / 关键词 | `_config.yml`（`# Site` 段） |
| 改站点 URL / 链接格式 | `_config.yml`（`# URL` 段） |
| 换主题 / 改搜索配置 | `_config.yml`（`theme`、`search` 段） |
| 改导航菜单 / 头像 / 社交链接 / favicon | `themes/butterfly/_config.yml` |
| 挂载自定义 CSS / JS | `themes/butterfly/_config.yml` → `inject:` 段 |
| 写 / 改 / 删文章 | `source/_posts/` 下的 `.md` 文件 |
| 给文章分类 / 打标签 | 文章 front matter 的 `categories` / `tags` |
| 改欢迎动画效果 | `source/css/welcome_animation.css` + `source/js/welcome_animation.js` |
| 改页首封面图 | `themes/butterfly/_config.yml` 的 `index_img` / `default_top_img`，图放 `source/img/covers/` |
| 改侧边栏天气卡片 | `source/js/weather.js` + `source/_data/widget.yml` |
| 换头像 | 覆盖 `source/img/avatar.png` |
| 改新建文章时的默认格式 | `scaffolds/post.md` |

---

## 4. 文章怎么写

### 4.1 新建文章

```bash
npx hexo new "文章标题"
```

会自动套用 `scaffolds/post.md` 的模板，生成到 `source/_posts/文章标题.md`。

### 4.2 front matter 约定

```markdown
---
title: 文章标题
date: 2026-08-12 00:00:00
categories: 技术          # 单个分类；分类会自动生成 /categories/技术/ 页面
tags: [js, hexo]          # 可多个
---
```

- **分类**：只写一个为主。所有分类统一走 `/categories/` 入口，不需要手动建页面。
- **标签**：零个或多个，用 `[a, b]` 或换行列表。
- 文章里放图片：把图片放到 `source/img/`，用 `/img/xxx.png` 引用。

---

## 5. 自定义功能是怎么挂载的

所有自定义 CSS / JS 都通过主题配置的 `inject:` 段挂载到全站页面：

`themes/butterfly/_config.yml`（约 1047 行）：
```yaml
inject:
  head:
    - <link rel="stylesheet" href="/css/welcome_animation.css">
    - <link rel="stylesheet" href="/css/custom.css">
  bottom:
    - <script src="/js/welcome_animation.js"></script>
    - <script src="/js/weather.js"></script>
    - <script src="/js/carousel.js"></script>
```

对应文件的作用：

| 文件 | 作用 |
|---|---|
| `js/welcome_animation.js` + `css/welcome_animation.css` | 首页欢迎文字动画 |
| `js/weather.js` + `_data/widget.yml` | 侧边栏天气卡片（固定郑州，无输入框） |
| `js/carousel.js` | 首页轮播图（手动翻阅，构建时随机顺序）+ 分类磁贴 |

要新增自定义脚本/样式：把文件放进 `source/js/` 或 `source/css/`，再在 `inject:` 段加一行即可。

---

## 6. Git 规则

- `.gitignore` 已覆盖：`node_modules/`、`public/`、`.deploy_git/`、`db.json`、`.claude/`、`指令.txt` 等。**不要**手动提交这些。
- 每次改动做成小提交，`git add` 只加本次涉及的文件，不要 `git add .`。
- 提交信息用中文写清「做了什么 + 为什么」，例如 `fix: 修复 favicon 404`。
- `themes/butterfly/` 目前整体被 git 跟踪（vendor 方式）。升级主题时会有一大段 diff，属正常现象；也可以后续改为 npm 依赖管理（见「已知问题」）。

---

## 7. 给 AI 协作者的快速指引

- 先读本文档再动手，不要凭猜测改文件。
- **只改该改的文件**：站点配置在 `_config.yml`，主题配置在 `themes/butterfly/_config.yml`，文章在 `source/_posts/`，自定义样式脚本在 `source/css` / `source/js`。
- 分类用 front matter 的 `categories`，**不要**在 `source/` 下新建分类目录。
- 改配置后提醒用户跑 `npm run clean && npm run build` 验证。
- 不要改 `public/`、`db.json`、`node_modules/`、`themes/butterfly/layout/`（除非明确要求）。
- 需要「去 AI 味」（清理模板感样式/文案）时，可用已安装的 `kill-ai-slop` 技能，先扫描报告、用户确认后再动手。

---

## 8. 已知问题 / TODO

已解决（历史记录，勿再改回）：

- [x] ~~favicon 缺失~~：现已修复为 `source/img/favicon.png`（真 PNG，8.2KB）。注意：不要用 .ico 伪装成 .png 的文件，浏览器和构建工具都不认。
- [x] ~~URL 未配置~~：`_config.yml` 的 `url` 已配置为 `https://11408-prog.github.io/azur_blog`。
- [x] ~~部署未配置~~：`deploy.type: git` 已配置，`npm run deploy` 可直接发布到 gh-pages 分支。
- [x] ~~图片格式坑~~：`life.jpg` / `love.jpg` 曾是真 HEIF 内容（iPhone 导出），Chrome/Edge 显示为破图，已转成真 JPEG。以后加图注意：`sharp metadata` 验证真实格式与扩展名一致。

仍待处理：

- [ ] **无 CI**：`.github/workflows/` 为空，没有自动构建/发布流程。
- [ ] **主题管理方式**：`themes/butterfly/` 以 vendor 方式在 git 里，升级麻烦；可考虑改为 npm 包 `hexo-theme-butterfly` 或 git submodule。
- [ ] **README 未完善**：`README.md` 目前几乎是空的，可作为对外介绍补充。
- [ ] **_img_backup 未忽略**：`source/_img_backup/` 是图片备份目录，建议加入 `.gitignore`，不要提交进仓库。
