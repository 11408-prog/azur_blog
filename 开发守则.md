# 开发守则（博客维护指南）

> 目标读者：任何人 / 任何 AI 协作者。
> 看完本文档后，你应该能回答两个问题：**博客结构是什么**、**想改某个东西该改哪个文件**。
> 源码地址： https://github.com/11408-prog/azur_blog
> 博客链接(1)： https://11408-prog.github.io/azur_blog/
> 博客链接(2)：https://azurlane-enterprise.netlify.app
> 博客链接(3)：https://azurlane-enterprise.pages.dev/

- 技术栈：**Hexo 8**（静态博客框架）+ **Butterfly** 主题（深度定制版）
- 内容格式：Markdown，存放在 `source/_posts/`
- 构建产物：`public/`（自动生成，不要手动改，已在 `.gitignore` 中）
- 公式渲染：**MathJax 4 服务端渲染 → SVG**（`hexo-filter-mathjax`，构建期输出矢量图，无前端脚本、无字体依赖）

---

## 1. 常用命令

| 命令 | 作用 |
|---|---|
| `npm run build` | 生成静态站点到 `public/`（等价 `hexo generate`） |
| `npm run server` | 本地预览，访问 `http://localhost:4000` |
| `npm run clean` | 清理 `public/` 与 `db.json` 缓存（改配置后最好跑一次） |
| `npm run deploy` | 部署到 GitHub Pages（gh-pages 分支） |
| `npm test` | 校验构建产物（`test/validate-build.js`，**部署前建议跑一次**，需先 build） |
| `npx hexo new "文章标题"` | 新建文章到 `source/_posts/` |
| `node compress-images.js` | 批量压缩图片（备份原图到 `source/_img_backup/`） |
| `node scripts/daily-log.js` | 手动写一条 daily_log（构建时自动记录，无需手动跑） |
| `auto-deploy.cmd` | **一键部署**：构建 → GitHub Pages → 推源码 → Cloudflare Pages →（可选）Netlify（推荐，见第 11 节） |
| `netlify login` | 登录 Netlify 账号（首次使用） |
| `netlify init` | 关联本地项目与 Netlify 站点（首次使用） |
| `netlify deploy --dir=public --prod` | 将 `public/` 部署到 Netlify 生产环境 |
| `netlify open` | 在浏览器打开 Netlify 站点控制台（可选） |

标准流程：`npm run clean && npm run build`，然后 `npm run server` 本地查看。  
部署时直接运行 `auto-deploy.cmd`（实际执行顺序见第 11 节）。  
如果只想单独部署 Netlify，确保 `public/` 是最新构建产物后运行 `netlify deploy --dir=public --prod`。

### 1.1 构建产物校验（`npm test`）

`test/validate-build.js` 在 `npm run build` 之后运行，检查 10 项，任一失败退出码为 1：

| 检查项 | 防的是什么 |
|---|---|
| `gallery.json` 存在，且每项含 `url` / `w` / `h`，**非空数组** | 画廊瀑布流塌陷、线上空画廊 |
| `data/activity.json` 是对象而非数组，key 格式为 YYYY-MM-DD | 活跃度热力图渲染失败 |
| `carousel-list.json` 含 `images` 数组 | 轮播/磁贴空白 |
| `img/avatar.png`、`img/favicon.png`、`videos/cover.mp4`、`music/bgm.mp3` 存在 | 线上 404 |
| `_config.yml` 的 `root` 为 `/azur_blog/` | **前端 JS 写死了该前缀**，改了 root 会全站资源错位 |
| `_config.yml` 的 `url` 与 `root` 一致性 | URL 拼接错误导致链接全错 |
| inject 引用的本地 JS/CSS 文件在 `public/` 中存在 | 自定义脚本/样式加载失败 |
| inject 本地资源均带 `?v=` 版本号 | **改了文件但浏览器用缓存，改动不生效**（第 6 节） |
| 关键页面 HTML 生成（首页/画廊/分类/标签） | 构建过程静默跳过某些页面 |
| 文章 front matter 含 `title` / `date` / `categories` | 文章不出现在分类页、活跃度统计漏算 |
| 图片格式真实性（防 HEIF 伪装 .jpg/.png） | iPhone 拍摄的图片不显示 |

> 它覆盖第 11 节那个「线上画廊空」的大坑，部署前跑一次能提前发现问题。  
> `test/` 目录已入库（2026-09-12 从 `.gitignore` 移除），换机器也能用。

**便捷命令：**

| 命令 | 作用 |
|---|---|
| `npm test` | 单独运行校验（需先 build） |
| `npm run test:build` | 一键 clean + build + test（推荐日常使用） |
| `npm run test:quick` | 快速模式（跳过文章扫描等耗时项） |
| `npm test -- --strict` | 严格模式（警告也视为失败） |

**部署脚本集成：** `auto-deploy.cmd` 会在构建之后、部署之前自动运行校验（`node test/validate-build.js`），失败则中断部署，不会把有问题的构建产物发布出去。

## 2. 目录结构（当前真实状态）

```
blog/
├── _config.yml                  ← 站点配置（标题、URL、主题、搜索）
├── package.json                 ← Node 依赖与 npm 脚本
├── node_modules/                ← 已安装依赖（git 忽略）
│
├── scripts/                     ← Hexo 构建插件脚本（会被自动加载）
│   ├── carousel.js              （生成 /carousel-list.json：轮播/画廊图片列表）
│   ├── gallery-list.js          （生成 /gallery.json：画廊图片列表，扫描 source/carousel/）
│   ├── activity.js              （生成 /data/activity.json：按天统计文章数）
│   ├── daily-log.js             （构建/部署事件写入 daily_log/；`hexo server` 本地会话下额外记录耗时/文件数/改动文件/报错，见第 8.2 节）
│   └── netlify-redirects.js     （生成 /_redirects：把 /azur_blog/* 重写到根路径，让 Netlify 部署在 / 也能正常加载资源；GitHub Pages 忽略此文件）
│
├── source/                      ← 所有「原料」都在这里
│   ├── _posts/                  ★ 文章（Markdown，按主题建子目录，分类走 front matter）
│   ├── carousel/                ★ 画廊图片（丢图进这里 + 重新构建 = 自动进画廊，git 忽略）
│   ├── gallery/index.md         「画廊」页面（瀑布流 + 随机排列）
│   ├── activity/index.md        「活跃度」页面（独立 layout，非首页热力图）
│   ├── categories/index.md      「分类」聚合页（type: categories，Hexo 原生）
│   ├── tags/index.md            「标签」聚合页（type: tags，Hexo 原生）
│   ├── music/                   bgm.mp3（首页 BGM）+ index.md（音乐页，用外部歌单，见第 12 节）
│   ├── videos/                  页首视频封面（cover.mp4）
│   ├── img/                     ← 图片资源
│   │   ├── avatar.png           （头像）
│   │   ├── phone_cover.jpg      （手机端页首封面）
│   │   ├── covers/              （页首封面图，当前用 enterprise_2.jpg）
│   │   ├── 文章封面/             （文章页 header 封面，默认封面 if.jpg）
│   │   └── 汇总-立绘/            （皮肤立绘图）
│   ├── css/                     ← 自定义样式（按模块拆分，见第 5 节）
│   ├── js/                      ← 自定义脚本（按模块拆分，见第 5 节）
│   └── _data/widget.yml         ← 侧栏自定义卡片（天气）
│
├── themes/butterfly/            ← 主题本体（vendor 进 git，深度定制）
│   ├── _config.yml              ★★ 主题配置（最常改的文件之一，含 inject / 齿轮内联脚本）
│   └── layout/includes/mixins/indexPostUI.pug
│                                ★ 首页文章卡片模板（已改为竖版 2 列结构）
│
├── test/validate-build.js       ← 构建产物校验脚本（npm test，已入库，见第 1.1 节）
├── lib/node-logger.js           ← 构建端日志工具（被 scripts/ 引用）
├── daily_log/                   ← 每日构建日志（自动生成，git 忽略，保留 7 天）
├── public/                      ← 构建产物（自动生成，git 忽略）
├── db.json                      ← Hexo 缓存（git 忽略）
├── compress-images.js           ← 批量压缩图片（根目录，非 scripts/，见第 1 节命令表）
├── convert-skins.js             ← 皮肤立绘 PNG/JPG 批量转 WebP（根目录，非 scripts/——
│                                  放进 scripts/ 会被 Hexo 每次构建自动执行，反复转换）
├── .agents/skills/kill-ai-slop/ ← 「去 AI 味」技能（见第 10 节），随 skills-lock.json 锁定版本
├── skills-lock.json             ← .agents/skills/ 下技能的来源与哈希锁定，勿手动改哈希
├── .github/dependabot.yml       ← npm 依赖自动更新（每日扫，独立于已删的 CI，仍在生效）
├── .gitignore
├── 需要解决的问题.md              ← 技术债/待办清单（**可能滞后于代码，见下方说明**）
└── 开发守则.md                   ← 本文档
```

> 注意：`source/_img_backup/` 是图片备份目录（git 忽略）。`source/技术/` 等空目录已删除，**分类统一走 Hexo 原生机制**（文章 front matter 写 `categories`），不要手动建分类目录。

> **`需要解决的问题.md` 可能滞后于代码**：这份清单记录的是发现问题时的状态，修复后**不保证有人回头把它标记为已解决**。已经出现过清单里写着「P0/P1 待修复」，但对应源文件（`logger.js`、`activity.js`、`random_posts.js`、`blog-config.js`、`weather.js`）其实早就修完、并且代码注释里直接写了「见《需要解决的问题.md》P1-x」的情况。**动手修复清单里的问题前，先去对应源文件搜一下该问题的关键词/编号（如 `P1-6`），确认它是不是已经修过了**，避免做重复劳动或把已经写好的方案改回旧版本。

---

## 3. 想改什么 → 改哪个文件

| 你想做的事 | 改哪个文件 |
|---|---|
| 改站点副标题 / 描述 / 关键词 | `_config.yml`（`# Site` 段） |
| 改站点 URL / 链接格式 | `_config.yml`（`# URL` 段） |
| 改导航菜单 / 头像 / 社交链接 | `themes/butterfly/_config.yml`（`menu:` / `avatar:` / `social:`） |
| 挂载 / 卸载自定义 CSS / JS | `themes/butterfly/_config.yml` → `inject:` 段 |
| 改首页文章卡片（样式） | `source/css/post-list.css` |
| 改首页文章卡片（结构/元信息） | `themes/butterfly/layout/includes/mixins/indexPostUI.pug` |
| 改画廊页（瀑布流样式） | `source/css/gallery.css` |
| 改画廊图片列表（加图） | 图丢进 `source/carousel/`，重新构建即可（自动扫描） |
| 改画廊图片顺序/随机 | `source/js/gallery.js`（洗牌逻辑） |
| 改首页活跃度热力图 | `source/js/activity.js` + `source/css/activity.css` |
| 改活跃度数据统计口径 | `scripts/activity.js`（构建时按文章日期生成 JSON） |
| 改欢迎动画 | `source/css/welcome_animation.css` + `source/js/welcome_animation.js` |
| 改页首封面图 | `themes/butterfly/_config.yml` 的 `index_img` / `default_top_img` |
| 改手机端页首封面 | `source/img/phone_cover.jpg`（换图即可）+ `source/js/video-cover.js` |
| 改 BGM | `source/js/music.js`（音频在 `source/music/bgm.mp3`） |
| 改齿轮设置菜单（暗色/BGM 开关） | `themes/butterfly/_config.yml` → inject 里的齿轮脚本 |
| 改侧边栏天气卡片 | `source/js/weather.js` + `source/_data/widget.yml` |
| 改文章卡片随机排序/置顶列表 | `source/js/random_posts.js`（`PINNED` 数组） |
| 改阅读模式（侧栏只留目录） | `source/css/reading-mode.css` |
| 改日志输出（浏览器端） | `source/js/logger.js`（DSLog 工具，含页面内调试面板，见第 8.1 节） |
| 改构建/本地日志 | `scripts/daily-log.js`（见第 8.2 节） |
| 改站点路径前缀（`/azur_blog/`）的派生方式 | `source/js/blog-config.js`（`BlogConfig.url()`，全站前端脚本统一从这里取路径，不再手写字面量） |
| 改 PJAX 页面切换后的功能重新挂载方式 | `source/js/lifecycle.js`（`BlogLifecycle`，见第 9 节，新增动态 DOM 的功能都应该接入这个而不是自己监听 `pjax:complete`） |
| 改公式渲染方式 | `_config.yml` 的 `mathjax:` 段 + `themes/butterfly/_config.yml` 的 `math:` 段（见第 5.3 节） |
| 文章里的公式写法 | 直接写 LaTeX：行内 `$...$`、块级 `$$...$$`（无需 front matter，已全站启用） |
| 换头像 | 覆盖 `source/img/avatar.png` |
| 改新建文章时的默认格式 | `scaffolds/post.md` |
| 写 / 改 / 删文章 | `source/_posts/` 下的 `.md` 文件 |

> **关于站点标题**：`_config.yml` 的 `title: ''` 是**有意留空**的（站名靠 subtitle 与页首视觉呈现，不用 h1 文字）。副作用是 `<title>` 会输出成「 - 欢迎来到灰色幽灵的博客 -」——开头多一个「 - 」，因为 Butterfly 的 `head.pug` 第 14 行拼副标题时固定加了 `' - '` 前缀。若想去掉，改 `_config.yml` 的 `subtitle`（删掉它开头的空格与结尾的「 - 」），不要动 `title`。

---

## 4. 文章怎么写

### 4.1 新建文章

```bash
npx hexo new "文章标题"
```

### 4.2 front matter 约定

```markdown
---
title: 文章标题
date: 2026-08-12 00:00:00
categories: 技术          # 单个分类；自动生成 /categories/技术/ 页面
tags: [js, hexo]          # 可多个
cover: /img/xxx.jpg       # 可选：首页卡片封面图（不加则卡片无封面）
sticky: 1                 # 可选：置顶（数字越大越靠前）
---
```

- **分类**：通常选择“学习”或“热爱”。
- **封面**：不写 `cover` 的卡片显示默认封面（位置在\source\img\文章封面\if.jpg）。
- 文章里放图片：图片放 `source/img/`，用 `/img/xxx.png` 引用（注意用半角 `![...](...)`，全角标点会导致图片不渲染）。

---

## 5. 自定义功能是怎么挂载的

所有自定义 CSS / JS 都通过主题配置的 `inject:` 段挂载（`themes/butterfly/_config.yml`）。**CSS 放 `head`，JS 放 `bottom`（按顺序加载）**。

### 5.1 自定义脚本（`source/js/`）

| 文件 | 作用 |
|---|---|
| `logger.js` | **必须第一个加载**。统一日志工具 `DSLog`（debug/info 默认隐藏，`DSLog.enableDebug()` 或 URL 加 `?debug` 开启）。开启调试模式后会额外在页面左下角出现一个悬浮日志面板（触摸设备无控制台时也能直接看日志、导出、清空），见第 8.1 节 |
| `blog-config.js` | **必须第二个加载**（`logger.js` 之后、所有业务脚本之前）。统一站点路径配置 `BlogConfig`，把 Hexo 的 `root` 派生成 `BlogConfig.url(p)`，替代各文件里手写 `/azur_blog/` 前缀 |
| `lifecycle.js` | **必须第三个加载**（`blog-config.js` 之后、所有业务脚本之前）。统一 PJAX 生命周期管理器 `BlogLifecycle`，提供 `register(name, {mount, destroy, persistent})`，自动回收模块内部的定时器/监听器/DOM 节点，详见第 9 节 |
| `welcome_animation.js` | 首页欢迎动画（灰色幽灵 + 打字机台词）。已接入 `BlogLifecycle`，打字机的定时器链改用 `ctx.timeout` 自动回收 |
| `weather.js` | 侧边栏天气卡片（固定郑州）。已接入 `BlogLifecycle`（`persistent: true`，数据缓存与 UI 渲染分离，PJAX 后只重绘不重新请求） |
| `carousel.js` | 画廊页轮播 / 首页磁贴。已接入 `BlogLifecycle` |
| `activity.js` | 首页活跃度热力图（**Git 提交记录版**，四季横排、无跨季）。统计起始日期取自 `data/activity.json` 里实际最早的一天，不再硬编码具体日期。已接入 `BlogLifecycle` |
| `random_posts.js` | 首页文章随机排序，置顶判断读取构建期渲染的 front matter 标记（`i.sticky`），不再按标题字符串匹配 |
| `gallery.js` | 画廊页瀑布流渲染（随机排列 + 灯箱）。已接入 `BlogLifecycle`，resize 防抖也改用 `ctx.on`/`ctx.timeout` |
| `video-cover.js` | 页首视频封面（手机端自动换 `phone_cover.jpg` 竖图）。已接入 `BlogLifecycle` |
| `music.js` | BGM 播放器（APlayer 1.x，注意其 `play()/pause()` 不返回 Promise）。已接入 `BlogLifecycle`（`persistent: true`，全局只创建一次实例，PJAX 后只做健康检查+UI 同步，不重建） |
| `toc-check.js` | 目录检查（控制侧栏卡片显示）。已接入 `BlogLifecycle` |

> **迁移状态**：以上除 `logger.js`/`blog-config.js`/`lifecycle.js`/`random_posts.js` 外的全部业务模块已迁移到 `BlogLifecycle`，全站只有一个 `pjax:complete` 监听入口（`lifecycle.js` 内部）。`random_posts.js` 本身不涉及定时器/监听器，暂未接入，不影响功能。

### 5.2 自定义样式（`source/css/`）

| 文件 | 作用 |
|---|---|
| `variables.css` | **色板变量**（改颜色只改这里）：`--navy-deep` / `--navy-mid` / `--gold` 等 |
| `layout-fix.css` | 全局布局微调 + 画廊页特殊样式 |
| `post-list.css` | 首页文章卡片（竖版 2 列网格） |
| `gallery.css` | 画廊瀑布流样式 |
| `activity.css` | 活跃度热力图样式 |
| `sidebar.css` | 侧栏卡片样式 |
| `reading-mode.css` | 阅读模式（文章页侧栏只留目录） |
| `carousel.css` / `tiles.css` / `weather.css` / `welcome_animation.css` / `music-page.css` / `custom.css` | 对应组件样式 |

要新增脚本/样式：文件放进 `source/js/` 或 `source/css/`，在 `inject:` 段加一行，**并记得升级版本号（见第 6 节）**。

### 5.3 公式渲染（MathJax 服务端 SVG）

- **方案**：`hexo-filter-mathjax` 在**构建期**用 MathJax 4 把 LaTeX 渲染成**内联 SVG**（矢量图）——无前端 JS、无字体文件依赖，页面加载即完整显示。
- **渲染器**：`hexo-renderer-markdown-it`（**纯** markdown-it）。
  ⚠️ **不要再装 `hexo-renderer-markdown-it-katex`**：它内置 KaTeX 插件抢渲染，且硬编码注入 `cdn.bootcss.com` 的 KaTeX CSS——该 CDN 已停服，会导致公式符号空白（详见第 13 节已解决记录）。
- **配置**：
  - `_config.yml` → `mathjax:`：`every_page: true`（全站启用，无需逐篇 front matter）、`append_css: true`（注入本地 CSS）、`single_dollars: true`
  - `themes/butterfly/_config.yml` → `math.use` 留空、`katex.enable: false`（**关闭主题前端数学渲染**，避免与 MathJax 冲突）
  - `_config.yml` → `markdown.plugins` 留空（不挂 katex 插件）
- **写法**：行内 `$F_1(x)$`、块级 `$$...$$` 即可，不必加 `mathjax: true`。
- **副作用**：构建变慢属正常（服务端渲染较重），公式密集的文章尤其明显；产出 HTML 体积略增（内联 SVG）。

---

## 6. 资源版本号规则（非常重要）

`inject:` 里所有资源都带 `?v=1.2` 版本号。**每次修改自定义 CSS/JS 后，必须把对应资源的版本号 +1**（`v=1.2` → `v=1.3`），否则浏览器用缓存、改动不生效。

> 这个坑已经踩过四次（WebP 不显示、设置菜单消失、手机封面不生效等），都是版本号没升。**改完文件 → 升版本号 → 重新构建**，三步缺一不可。

---

## 7. Git 规则

- `.gitignore` 已覆盖：`node_modules/`、`public/`、`.deploy_git/`、`db.json`、`.claude/`、`指令.txt`、`source/_img_backup/`、`daily_log/`。**不要**手动提交这些。
- **手动提交时**：每次改动做成小提交，`git add` 只加本次涉及的文件，不要 `git add .`。提交信息用中文写清「做了什么 + 为什么」。
- **例外**：`auto-deploy.cmd` 会执行 `git add .` + 自动提交（信息为 `Auto deploy: 日期 时间`）。这是脚本行为，代价是提交历史里没有「改了什么、为什么」的信息。**日常开发请手动提交，只在发布时跑 auto-deploy**；若希望自动提交也有信息量，需改脚本里 `git commit -m "Auto deploy: %date% %time%"` 这一行（具体行号会随脚本改动变化，直接搜这行文本定位，不要死记行号）。脚本会先用 `git diff --cached --quiet` 判断有没有真正的改动，没有改动就跳过 commit，但**仍然会尝试 push**（避免上次 commit 成功但 push 失败、遗留在本地的提交被无限期搁置）。
- `themes/butterfly/` 以 vendor 方式被 git 跟踪。**我们改过它的文件**：`layout/includes/mixins/indexPostUI.pug`（卡片结构）、`_config.yml`（inject/菜单等）——升级主题前注意备份这些改动。

---

## 8. 日志与排查

### 8.1 浏览器端（DSLog）

- 所有自定义脚本用 `DSLog.info/warn/error('模块', '消息', 数据)` 输出统一格式日志。
- 默认只显示 warn/error；开调试看全部：`DSLog.enableDebug()` 或地址栏加 `?debug`。
- 全局错误捕获：未捕获 JS 错误和 Promise 拒绝会自动上报（模块 `global`）。
- **页面内调试面板**：开启调试模式后，页面左下角会出现一个悬浮按钮（🐞），点开是一个可滚动的日志列表，带导出/清空/关闭按钮——解决的是控制台在手机浏览器上默认打不开、调试信息实际上看不到的问题。面板只在调试模式开启时创建，对普通访客始终不可见。关掉调试模式（`DSLog.disableDebug()`）面板会一并移除。

### 8.2 构建端（daily_log）

- `scripts/daily-log.js` 在每次构建/部署时自动写入 `daily_log/YYYY-MM-DD.log`，**自动清理 7 天前的日志**，git 忽略。
- `npm run build` / `npm run deploy`（含 `auto-deploy.cmd` 里跑的那些）记录的还是极简的「构建开始/构建完成」两行，跟以前一样。
- **`npm run server` 本地会话下会额外记录**：本次构建耗时、生成的文件数与文章数、由哪个文件改动触发的增量构建（如 `改动: _posts/xxx.md`）、以及构建过程中真正的 `[WARN]`/`[ERROR]` 内容（挂在 `hexo.log` 上，不影响控制台原有输出）。
  > 已知小瑕疵：文件改动到「归因到日志里哪一行」偶尔会错位一格（比如编辑器触发了两次保存事件，中间夹着一次防抖构建），不影响构建本身，只是「改动: xxx」这行辅助信息偶尔对不准触发它的那次构建。
- 排查构建问题：看 `daily_log/` 当天文件；本地开发时报错信息现在会直接出现在这份日志里，不用再翻终端滚动的输出。

---

## 9. PJAX 注意事项（容易踩坑）

博客开启了 PJAX（页面无刷新切换），`#body-wrap` 会被整体替换。**凡是往页面里动态注入 DOM 的自定义脚本，都必须在 PJAX 完成后重新执行**，否则切换页面后功能消失。

**标准做法：接入 `BlogLifecycle`**（`source/js/lifecycle.js`，见第 5.1 节）。写法：

```js
window.BlogLifecycle.register('模块名', {
  mount: function (ctx) {
    // 首次挂载 / PJAX 后重新挂载
    // 用 ctx.on / ctx.interval / ctx.timeout / ctx.observer / ctx.append 申请资源
    // 会在销毁时自动回收，不用自己手写清理逻辑
  },
  destroy: function (ctx) { /* 可选：处理第三方实例等 ctx 管不到的东西 */ },
  persistent: false  // 全局常驻组件（如播放器、天气卡片）设 true，PJAX 后只 refresh 不销毁重建
});
```

好处：全站只有一个 `pjax:complete` 监听入口，资源自动回收，不容易因为忘记清理定时器/监听器导致 PJAX 反复切换后内存泄漏或重复触发。**现在 `welcome_animation.js`、`weather.js`、`carousel.js`、`activity.js`、`gallery.js`、`video-cover.js`、`music.js`、`toc-check.js` 全部已经接入**，新增任何涉及动态 DOM 的功能都应该走这条路，不要再自己单独写：

```js
document.addEventListener('pjax:complete', init); // 旧写法，不再新增
```

已知易错点：PJAX 后元素是**新节点**（旧引用失效），事件要用动态查询；容器渲染前先 `innerHTML = ''` 防叠加。

> **踩过的坑**：`lifecycle.js` 和 `blog-config.js` 曾经只是写好了代码放在 `source/js/` 里，但**忘了把它们加进 `themes/butterfly/_config.yml` 的 `inject.bottom` 列表**，导致 `window.BlogLifecycle` / `window.BlogConfig` 在线上一直是 `undefined`，所有引用它们的模块全部悄悄走了兜底分支，新架构形同虚设，且没有任何报错提示。**新增 `source/js/` 文件后，一定要去 `inject.bottom` 里加对应的 `<script>` 标签，并跑 `npm run test:build` 确认「inject 引用的资源确实存在于构建产物里」这一项检查通过**（第 1.1 节）。

---

## 10. 给 AI 协作者的快速指引

- 先读本文档再动手，不要凭猜测改文件。
- **只改该改的文件**：站点配置 `_config.yml`，主题配置 `themes/butterfly/_config.yml`，文章 `source/_posts/`，自定义样式脚本 `source/css` / `source/js`。
- 分类用 front matter 的 `categories`，不要在 `source/` 下新建分类目录。
- 改完任何自定义 CSS/JS：**升级 `inject:` 里的版本号 +1**，提醒用户强刷（Ctrl+Shift+R）。
- 涉及动态注入 DOM 的功能：**优先用 `BlogLifecycle.register()`**（见第 9 节），不要再自己单独监听 `pjax:complete`。
- 新增 `source/js/` 或 `source/css/` 文件后：**必须**去 `themes/butterfly/_config.yml` 的 `inject:` 段加对应的 `<script>`/`<link>` 标签，写完代码不代表它会被加载——这个坑已经真实踩过一次（见第 9 节末尾），加完后跑 `npm run test:build` 确认校验通过。
- 改配置后提醒用户跑 `npm run clean && npm run build` 验证。
- 不要改 `public/`、`db.json`、`node_modules/`、`daily_log/`、`source/_img_backup/`。
- 需要「去 AI 味」时，可用 `kill-ai-slop` 技能，先扫描报告、用户确认后再动手。
- 除非特殊说明或者经过明确确认，否则不要加毛玻璃。
- 处理 `需要解决的问题.md` 里的条目前，先核实是否已经修复（见第 2 节末尾说明）；确认修复后，把该条目改成「✅ 已解决」并简述修复方式（格式参考本文档第 13 节「已解决（历史记录，勿再改回）」），**不要直接删除条目**——保留记录能防止以后又被同一个问题绕进去。反过来，发现新的技术债，也应该补充进 `需要解决的问题.md` 而不是只在对话里提一句就过去。
- 修改 `scripts/` 目录下任意文件后（`activity.js`/`carousel.js`/`daily-log.js`/`gallery-list.js`）：**必须**跑一次 `npm run test:build`，确认生成的 JSON 结构仍能通过 `test/validate-build.js` 的 10 项校验（第 1.1 节）——这些脚本参与构建期数据生成，JS 不报错不代表数据结构没变，前端可能悄悄失效。

---

## 11. 部署说明与排障

### 双仓库机制（务必理解）

- **`main` 分支**：源码仓库（md/脚本/配置，**不含画廊图**——`source/carousel/` 已 gitignore，图只在本机）
- **`gh-pages` 分支**：网页仓库（`npm run deploy` 推送的 `public/` 构建产物，含压缩后的图）
- GitHub Pages **只显示 gh-pages**；main 里有没有图与网页无关

### 部署流程（auto-deploy.cmd 一键完成）

脚本**实际执行顺序**（以 `auto-deploy.cmd` 为准）：

1. `hexo clean`（清理 `public/` 与 `db.json`）
2. `hexo generate -c 4`（4 并发构建；失败则自动降级 `hexo generate -c 1` 串行重试）
3. `node test/validate-build.js`（构建产物校验，见第 1.1 节；失败中断部署）
4. `hexo deploy`（把 `public/` 强推 gh-pages，更新 GitHub Pages；失败仅警告，不中断）
5. `git add .` + （有改动才）`git commit -m "Auto deploy: 日期 时间"` + `git push origin main`（推源码；commit 没有改动会跳过，但只要 commit 没失败就总会尝试 push；失败仅警告）
6. `wrangler pages deploy ./public --project-name=azurlane-enterprise --branch=main`（部署 Cloudflare Pages；找不到 wrangler 或部署失败**仅警告，不中断**）
7. 可选：`netlify deploy --dir=public --prod`（运行前会询问是否部署；找不到 netlify-cli 或失败**仅警告，不中断**）

> 注意顺序是**先构建部署产物、后推源码**，与直觉相反但无碍：两者内容相互独立。  
> **第 4、5、6、7 步全部是失败仅警告、不中断**——脚本"跑到底"不代表每一步都成功，结尾会打印一份汇总（`GitHub Pages` / `GitHub Source` / `Cloudflare Pages` / `Netlify` 各自的 `SUCCESS`/`FAILED` 状态），**看这份汇总而不是看脚本有没有报错退出**来判断有没有真的全部部署成功。  
> 只有第 1、2、3 步（清理/构建/校验）失败才会真正中断脚本——这几步失败意味着连能部署的产物都没有，中断是合理的。

### 为什么不用 CI

画廊图只在本机，云端 clone 不到 `source/carousel/`，**自动构建会得到空画廊并覆盖线上**。
CI（deploy.yml）已删除，只保留本地部署。

### 排障：线上画廊空 / 页面显示源码（踩过的大坑）

- **症状**：线上 `gallery.json` 是 `[]`，或页面内容看起来是源码而非构建产物
- **根因**：`.deploy_git` 目录的 `.git` 被删后，`hexo deploy` 找不到部署仓库的 git，会**向上误用主仓库的 `.git`**，把 **main 源码**强推到 gh-pages
- **修复**：删除**整个** `.deploy_git` 目录，重新 `npm run deploy`（hexo 会完整重建）
- **教训**：清部署历史必须删**整个 `.deploy_git`**，让 hexo 重建；只删 `.git` 会出大问题

### 排障：推送超时（HTTP 408）

- 原因：仓库体积大（图进 git / gh-pages 历史累积）
- 已配置：`git config http.postBuffer 524288000`（500MB，跟仓库走）
- 已处理：图 gitignore（main）+ gh-pages 历史清空（force 单提交）
- **不要设 `http.timeout`**（会掐断慢推送）；postBuffer 500MB 已足够

---

## 12. 外部依赖清单（挂了会影响什么）

站点有若干功能依赖第三方服务，**它们不受本仓库控制**，失效时表现为"某个功能突然坏了"。排查前先对照下表，别去翻自己的代码。

| 依赖 | 用途 | 引用位置 | 失效表现 |
|---|---|---|---|
| `fonts.loli.net` | Noto Serif SC 中文字体 | `inject.head` 第 1 行 | 字体回退为系统默认，版式略变，**不影响内容** |
| `cdn.jsdelivr.net` | APlayer / Meting 播放器库 | `inject.bottom` | 首页 BGM 与音乐页**均无法播放** |
| `api.injahow.cn` | Meting 歌单解析接口（第三方公共服务） | `inject.bottom` 内联 `window.meting_api` | **音乐页 `/music/` 歌单空**（网易云歌单 id `18308336739`）；首页 BGM 不受影响 |
| `wttr.in` | 侧栏天气数据（固定郑州） | `source/js/weather.js` 第 6 行 | 天气卡片无数据/显示异常 |
| GitHub Pages | 站点主访问入口 | `_config.yml` `deploy` | 主链接打不开；Netlify/Cloudflare 镜像仍可用 |
| Cloudflare Pages | 站点镜像 | `auto-deploy.cmd` 第 6 步（需要 `wrangler login`） | 镜像失效；GitHub Pages 仍可用 |
| Netlify | 站点镜像 | `auto-deploy.cmd` 第 7 步（可选） | 镜像失效；GitHub Pages 仍可用 |

要点：

- **首页 BGM 走本地文件**（`source/music/bgm.mp3`，由 `source/js/music.js` 加载），不依赖 Meting 接口——所以"音乐页坏了但首页还有声音"是正常现象，不是 bug。
- **公式不依赖任何 CDN**：历史上 KaTeX 走 BootCDN 导致符号空白，现已改为 MathJax 服务端渲染（构建期出 SVG），彻底摆脱外部字体/CDN（见第 5.3 节）。
- 天气与歌单都是**第三方免费接口，无 SLA**，偶发不可用属正常，不必改代码。若要长期稳定，天气可换和风/心知等带 key 的服务，歌单可改为纯本地音频列表。
- `hexo-tag-aplayer` 依赖已在 `package.json` 中保留（供 `{% aplayer %}` 标签使用），`_config.yml` 里 `aplayer.asset_inject: false` 表示不自动注入资源，由 `inject` 手动挂载。

---

## 13. 已知问题 / TODO

已解决（历史记录，勿再改回）：

- [x] ~~favicon 缺失~~：`source/img/favicon.png`（真 PNG，8.2KB）。不要用 .ico 伪装 .png。
- [x] ~~URL / 部署未配置~~：`url` 与 `deploy.type: git` 均已配置，`npm run deploy` 可发布。
- [x] ~~图片格式坑~~：曾有用 iPhone HEIF 内容伪装 .jpg 导致破图，已修。加图后用 `sharp metadata` 验证真实格式。
- [x] ~~_img_backup 未忽略~~：已加入 `.gitignore`。
- [x] ~~README 未完善~~：`README.md` 已重写，包含技术栈、快速开始、部署与目录结构说明。
- [x] ~~首页点文章分页「第 2 页」后再点「第 1 页」会强制刷新页面（PJAX 分页边界问题）~~：去掉 `pagination.pug` 中分页链接的 `#content-inner` 锚点，并在 `pjax.pug` 内联脚本的 `pjax:complete` 中手动滚动到 `#content-inner`（PJAX 初始化加 `scrollTo: false` 禁用自动滚动）。
- [x] ~~`check-ci.ps1` 死代码~~：已删除（2026-09-11）。它轮询 GitHub Actions 运行状态，而 CI 早已废弃，留着只会误导。
- [x] ~~`hexo-qidaink-cursor_effects` 无效依赖~~：已卸载（2026-09-11）。该插件默认 `enable: false`，且站点/主题配置里都没有 `cursor:` 段，入口直接 `return`、不注入任何东西。若将来想要光标特效，重新安装并在 `_config.yml` 加 `cursor: { enable: true, click: ..., move: ... }`。
- [x] ~~`test/` 未入库~~：已解决（2026-09-12）。从 `.gitignore` 移除 `test/`，校验脚本现在入库，换机器也能用。同时增强了脚本（检查 10 项而非 5 项），并集成到 `auto-deploy.cmd` 第 3 步自动运行。
- [x] ~~**公式符号不显示（数学公式渲染异常）**~~：根因是 `hexo-renderer-markdown-it-katex` 硬编码注入 `https://cdn.bootcss.com/KaTeX/0.11.1/katex.min.css`，而 **BootCDN 已停止服务** → KaTeX 样式与 20+ 个数学字体加载失败 → 符号空白。已改用 `hexo-filter-mathjax` 服务端 MathJax（**SVG 输出、零字体依赖**），并卸载 katex 渲染器、清理本地 KaTeX 资源。**以后不要再装 katex 相关渲染器/插件**（见第 5.3 节）。
- [x] ~~**两套置顶机制并存**~~：`random_posts.js` 现在读取构建期渲染的 front matter 标记（`i.sticky`）判断置顶，不再按标题字符串匹配 `PINNED` 数组，两套机制已合并为一套。
- [x] ~~**`music.js` 未带版本号**~~：已补上 `?v=` 版本号，跟其它 inject 资源一致。
- [x] ~~**`lifecycle.js` / `blog-config.js` 写了但没实际挂载**~~：两个文件曾经只是放在 `source/js/` 里，但忘了加进 `themes/butterfly/_config.yml` 的 `inject.bottom` 列表，导致 `window.BlogLifecycle` / `window.BlogConfig` 线上一直是 `undefined`，所有引用它们的模块全部悄悄走了兜底分支，且没有任何报错提示（详见第 9 节末尾的踩坑记录）。已修复：加入 `inject.bottom`，并把 `carousel.js`/`gallery.js`/`activity.js`/`video-cover.js`/`welcome_animation.js` 五个尚未接入的模块一并迁移到 `BlogLifecycle`（连同已提前完成的 `music.js`/`weather.js`/`toc-check.js`，全站业务模块迁移完毕）。**教训**：新增 `source/js/` 文件后，光写代码不够，一定要去 `inject.bottom` 加挂载并跑 `npm run test:build` 验证。

仍待处理：
- [ ] **齿轮菜单内联在 YAML 里**：`themes/butterfly/_config.yml` 第 1084-1310 行塞了 226 行 `<style>` + 脚本，与第 5 节「按模块拆分到 `source/css`、`source/js`」的原则相悖，也是全站最难改的地方。建议拆成 `settings-menu.css` / `settings-menu.js`（拆分后记得升版本号）。
- [ ] **主题管理方式**：`themes/butterfly/` 以 vendor 方式在 git 里，且被深度定制（卡片模板、inject），升级需谨慎。
- [ ] **画廊图片体积**：`source/carousel/` 当前 1788 张，含 1MB+ 大图（最大约 1.4MB），建议定期跑 `node compress-images.js`。
- [ ] **daily-log.js 的「改动文件」归因偶尔错位一格**：见第 8.2 节末尾的已知小瑕疵，不影响构建本身，暂不紧急。
- [ ] **markdown-it 全站排版行为切换的副作用未逐篇核实**：`breaks: true`（单个换行变 `<br>`）、`typographer: true`（自动转换引号/破折号等排版符号）是切换渲染器时顺带打开的全局配置，只验证过差分隐私第六章笔记这一篇渲染正常，其余文章尚未逐一核对排版有没有跟着变样，建议抽查几篇换行习惯随意的旧文章。
- [ ] **在手机上加载时网站封面上的字有些不明显**

说明：CI 已删除（有意）——画廊图只在本机，云端构建会空画廊；部署走本地 `npm run deploy`（见第 11 节）。`.github/dependabot.yml` **不是**死代码，它独立于 Actions 工作，仍每日扫描 npm 依赖提 PR。
