# 开发守则（博客维护指南）

> 目标读者：任何人 / 任何 AI 协作者。
> 看完本文档后，你应该能回答两个问题：**博客结构是什么**、**想改某个东西该改哪个文件**。
> 源码地址： https://github.com/11408-prog/azur_blog
> 博客链接(1)： https://11408-prog.github.io/azur_blog/
> 博客链接(2)：https://azurlane-enterprise.netlify.app
> 博客链接(3)：https://azurlane-enterprise.pages.dev/

> **核对基准**：本文档于 2026-09-21 对照 commit `3bca147` 的源码逐项核对，并实测过一次 `hexo generate` 与 `npm test`。
> 文中的「当前」「目前」都以该版本为准；无法在源码里验证的说法（第三方平台的行为、本机 git 配置等）会标注「（未验证）」。
> **改了行为却没同步本文，就是文档过期**——同步规则见第 14 节。文中不再引用行号（行号一改就作废），一律用文件名或搜索关键词定位。

- 技术栈：**Hexo 8**（`package.json` 中为 8.1.2）+ **Butterfly 5.7.0** 主题（vendor 进 git，深度定制）
- 内容格式：Markdown，存放在 `source/_posts/`
- 构建产物：`public/`（自动生成，不要手动改，已在 `.gitignore` 中）
- 公式渲染：**MathJax 4 服务端渲染 → SVG**（`hexo-filter-mathjax`，依赖 `@mathjax/src` 4.1.x；构建期输出矢量图，无前端脚本、无字体依赖）

---

## 1. 常用命令

| 命令 | 作用 |
|---|---|
| `npm run build` | 生成静态站点到 `public/`（等价 `hexo generate`） |
| `npm run server` | 本地预览，访问 `http://localhost:4000` |
| `npm run clean` | 清理 `public/` 与 `db.json` 缓存（改配置后最好跑一次） |
| `npm run deploy` | 部署到 GitHub Pages（`hexo deploy`，推到 gh-pages 分支） |
| `npm test` | 校验构建产物（`test/validate-build.js`，**部署前建议跑一次**，需先 build） |
| `npm run test:build` | 一键 clean + build + test（推荐日常使用） |
| `npm run test:quick` | 快速模式（只跳过「文章 front matter」这一项） |
| `npm test -- --strict` | 严格模式（警告也视为失败） |
| `node test/check-docs.js` | 检查本文档与源码是否一致（路径、命令、文件覆盖、章节引用；见第 14 节） |
| `npx hexo new "文章标题"` | 新建文章到 `source/_posts/`（`post_asset_folder: true`，同时会建同名资源文件夹） |
| `node compress-images.js` | 批量压缩图片（处理 avatar、favicon、study/life/love、`source/img/covers`、`source/carousel`；备份原图到 `source/_img_backup/`） |
| `node convert-skins.js` | 把 `source/img/汇总-立绘/` 下的 PNG/JPG 转成 WebP（会删除源文件，先备份到 `source/_img_backup/`） |
| `node -e "require('./scripts/daily-log').log('消息')"` | 手动写一条 daily_log（直接 `node scripts/daily-log.js` 什么也不会做——它是被 Hexo 加载的模块，构建时自动记录，无需手动跑） |
| `auto-deploy.cmd` | **一键部署**（Windows）：清理 → 构建 → 校验 → GitHub Pages → 推源码 → Cloudflare Pages →（可选）Netlify（推荐，见第 11 节） |
| `netlify login` | 登录 Netlify 账号（首次使用）（未验证） |
| `netlify init` | 关联本地项目与 Netlify 站点（首次使用）（未验证） |
| `netlify deploy --dir=public --prod` | 将 `public/` 部署到 Netlify 生产环境 |
| `netlify open` | 在浏览器打开 Netlify 站点控制台（可选）（未验证） |

标准流程：`npm run clean && npm run build`，然后 `npm run server` 本地查看。  
部署时直接运行 `auto-deploy.cmd`（实际执行顺序见第 11 节）。  
如果只想单独部署 Netlify，确保 `public/` 是最新构建产物后运行 `netlify deploy --dir=public --prod`。

### 1.1 构建产物校验（`npm test`）

`test/validate-build.js` 在 `npm run build` 之后运行。结果分三级：**错误**（退出码 1）、**警告**（默认不影响退出码，`--strict` 下才算失败）、通过。

完整模式正常情况下输出 **11 条**检查结果（`--quick` 少一条，为 10 条）；文章 front matter 不完整时，每篇多出一条警告。

| # | 检查项 | 不满足时的级别 | 防的是什么 |
|---|---|---|---|
| 1 | `_config.yml` 的 `root` 必须是 `/azur_blog/` | **错误** | 见下方说明：前端 JS 已不再写死该前缀，这条检查现在只是遗留的护栏 |
| 2 | `url` 以 `root` 结尾 | 警告 | URL 拼接错误导致链接全错 |
| 3 | `public/` 里存在 `img/avatar.png`、`img/favicon.png`、`videos/cover.mp4`、`music/bgm.mp3` | 警告 | 线上 404（**注意：只是警告，缺了也不会阻止部署**） |
| 4 | inject 引用的本地 JS/CSS 存在于 `public/` | **错误** | 自定义脚本/样式加载失败（第 9 节踩过的坑） |
| 5 | inject 本地资源都带 `?v=` | 警告 | 改了文件但浏览器用缓存（**只检查「有没有」，不检查「升没升」**，见第 6 节） |
| 6 | 首页、`gallery/`、`categories/`、`tags/` 的 `index.html` 已生成 | **错误** | 构建静默跳过某些页面 |
| 7 | 文章 front matter 含 `title` / `date` / `categories`（`--quick` 跳过） | 警告（每篇一条） | 文章不出现在分类页 |
| 8 | `public/img` 下 jpg/png 不是 HEIF 伪装（最多抽查 300 张） | **错误** | iPhone 拍摄的图片不显示 |
| 9 | `gallery.json`：存在、是数组、**非空**、每项有 `url` | **错误**；缺 `w`/`h` 为警告 | 画廊塌陷、线上空画廊（第 11 节的大坑） |
| 10 | `data/activity.json`：存在且是对象 | **错误**；空对象、key 不是 YYYY-MM-DD、前 10 个值不是数字为警告 | 热力图渲染失败 |
| 11 | `carousel-list.json`：存在且含 `images` 数组 | **错误** | 轮播/磁贴列表缺失（该文件目前没有页面在用，见 5.1） |

关于第 1 项：前端 JS 现在都从 `BlogConfig`（读取 Hexo 输出的 `GLOBAL_CONFIG.root`）派生路径，只在 `BlogConfig` 缺失时才回退到字面量 `/azur_blog/`。**现在真正把 `/azur_blog/` 写死的地方**是：`themes/butterfly/_config.yml` 的 `inject:` 里每一条本地资源地址，以及 `test/validate-build.js` 里的第 1、4、5 项检查。要改 `root`，这几处要一起改（`scripts/` 里的生成器读的是 `hexo.config.root`，会自动跟随）。

> `test/` 目录已入库（2026-09-12 从 `.gitignore` 移除），换机器也能用。  
> 它**不检查**的东西：外部 CDN 是否可用、画廊图片数量是否骤降（只检查非空）、inject 版本号是否升级、localStorage 键名是否一致。

**部署脚本集成：** `auto-deploy.cmd` 会在构建之后、部署之前运行 `node test\validate-build.js`（不带 `--strict`）：**出现「错误」才中断部署，「警告」不会**。

## 2. 目录结构（当前真实状态）

```
blog/
├── _config.yml                  ← 站点配置（标题、url/root、permalink、部署、搜索、MathJax、markdown-it）
├── package.json / package-lock.json
├── README.md                    ← 项目说明（文件编码是 UTF-16 LE，见第 13 节）
├── node_modules/                ← 已安装依赖（git 忽略）
│
├── scripts/                     ← Hexo 构建期脚本（Hexo 会自动加载并执行此目录下每个 .js）
│   ├── activity.js              （生成 /data/activity.json：按天统计 git 提交数，`git log --all`；git 不可用时回退为按文章 front matter 的 date 统计）
│   ├── carousel.js              （生成 /carousel-list.json：扫描 source/carousel/ 并随机打乱。**目前没有页面在用它**，见 5.1 与第 13 节）
│   ├── gallery-list.js          （生成 /gallery.json：扫描 source/carousel/，用 sharp 逐张读取宽高）
│   ├── daily-log.js             （构建/部署事件写入 daily_log/；`hexo server` 本地会话下额外记录耗时/文件数/改动文件/报错，见第 8.2 节）
│   └── netlify-redirects.js     （生成 /_redirects：`/azur_blog/*  /:splat  200`，让部署在根路径的镜像也能加载带前缀的资源；GitHub Pages 忽略此文件；Cloudflare Pages 镜像是否同样依赖它，脚本注释没写（未验证））
│
├── source/                      ← 所有「原料」都在这里
│   ├── _posts/                  ★ 文章（Markdown，按主题建子目录，分类走 front matter；子目录名会进入文章链接，见 4.3）
│   ├── _data/
│   │   ├── widget.yml           侧栏自定义卡片（天气）
│   │   └── butterfly.yml        空文件（Hexo 会把它当作主题配置的覆盖层，目前没有内容）
│   ├── carousel/                ★ 画廊图片（丢图进这里 + 重新构建 = 自动进画廊，git 忽略，只在本机）
│   ├── gallery/index.md         「画廊」页（type: gallery，无侧栏，正文只有一个 `<div id="masonry-gallery">`）
│   ├── activity/index.md        「活跃度」页（type: activity）：正文只有一个 `<div id="activity-chart">`，**没有任何脚本填充它，菜单里也没有入口**，见第 13 节
│   ├── categories/index.md      「分类」聚合页（type: categories，Hexo 原生）
│   ├── tags/index.md            「标签」聚合页（type: tags，带 custom_colors）
│   ├── music/                   bgm.mp3（首页 BGM）+ index.md（音乐页，`<meting-js>` 外部歌单，PJAX 排除页，见第 12 节）
│   ├── videos/cover.mp4         页首视频封面
│   ├── img/                     ← 图片资源
│   │   ├── avatar.png / favicon.png / background.jpg（全站背景）/ phone_cover.jpg（手机端页首封面）
│   │   ├── study.jpg / love.jpg （首页磁贴背景）
│   │   ├── qq.jpg / wechat.jpg  （社交菜单里 QQ / 微信 图标指向的图片）
│   │   ├── avatar1.png / life.jpg   （当前没有任何代码、配置或文章引用它们；life.jpg 只出现在 compress-images.js 的清单里）
│   │   ├── covers/enterprise_2.jpg  （页首封面，主题的 index_img / default_top_img 指向它）
│   │   ├── 文章封面/             （首页卡片没写 cover 时的默认封面，默认 if.jpg——路径写死在 indexPostUI.pug）
│   │   └── 汇总-立绘/            （皮肤立绘图，目前只有 1 篇文章引用）
│   ├── css/                     ← 自定义样式（按模块拆分，见第 5 节）
│   └── js/                      ← 自定义脚本（按模块拆分，见第 5 节）
│
├── themes/butterfly/            ← 主题本体 5.7.0（vendor 进 git）。相对上游被改过的文件只有 7 个，见第 7 节
│   ├── _config.yml              ★★ 主题配置（menu / avatar / index_img / theme_color / inject / pjax.exclude 等，最常改的文件之一）
│   └── layout/includes/mixins/indexPostUI.pug
│                                ★ 首页文章卡片模板（竖版 2 列结构，默认封面也在这里）
│
├── test/                        ← 校验脚本（已入库）
│   ├── validate-build.js        构建产物校验（npm test，见第 1.1 节）
│   └── check-docs.js            本文档与源码的一致性校验（见第 14 节）
├── lib/node-logger.js           ← 构建端日志工具。**目前没有任何文件引用它**（compress-images.js 等都没用），见第 13 节
├── auto-deploy.cmd              ← 一键部署脚本（见第 11 节）
├── compress-images.js           ← 批量压缩图片（根目录，非 scripts/，见第 1 节命令表）
├── convert-skins.js             ← 皮肤立绘 PNG/JPG 批量转 WebP（根目录，非 scripts/——
│                                  放进 scripts/ 会被 Hexo 每次构建自动执行，反复转换）
├── scaffolds/                   ← `hexo new` 的模板（post.md / page.md / draft.md）
├── .agents/skills/kill-ai-slop/ ← 「去 AI 味」技能（见第 10 节），随 skills-lock.json 锁定版本
├── skills-lock.json             ← .agents/skills/ 下技能的来源与哈希锁定，勿手动改哈希
├── .github/dependabot.yml       ← npm 依赖自动更新（每日扫，最多 20 个 PR，独立于已删的 CI，仍在生效）
├── .gitignore
├── 需要解决的问题.md              ← 技术债/待办清单（**可能滞后于代码，见下方说明**）
├── 开发守则.md                   ← 本文档
│
├── daily_log/                   ← 每日构建日志（自动生成，git 忽略，保留 7 天）
├── public/                      ← 构建产物（自动生成，git 忽略）
├── db.json                      ← Hexo 缓存（git 忽略）
│
└── cache_tmp/、true/、_update-notifier-last-checked
                                 ← **不是项目文件，是被误提交的杂物**（npm 缓存与 update-notifier 标记，cache_tmp/ 约 26MB、76 个文件已被 git 跟踪），见第 7、13 节
```

> 注意：`source/_img_backup/` 是图片备份目录（git 忽略）。`source/技术/` 等空目录已删除，**分类统一走 Hexo 原生机制**（文章 front matter 写 `categories`），不要手动建分类目录。

> **`需要解决的问题.md` 可能滞后于代码**：这份清单记录的是发现问题时的状态，修复后**不保证有人回头把它标记为已解决**。已经出现过清单里写着「P0/P1 待修复」，但对应源文件（`logger.js`、`activity.js`、`random_posts.js`、`blog-config.js`、`weather.js`）其实早就修完、并且代码注释里直接写了「见《需要解决的问题.md》P1-x」的情况。**动手修复清单里的问题前，先去对应源文件搜一下该问题的关键词/编号（如 `P1-6`），确认它是不是已经修过了**，避免做重复劳动或把已经写好的方案改回旧版本。

---

## 3. 想改什么 → 改哪个文件

| 你想做的事 | 改哪个文件 |
|---|---|
| 改站点副标题 / 描述 / 关键词 | `_config.yml`（`# Site` 段） |
| 改站点 URL / 链接格式 | `_config.yml`（`url` / `root` / `permalink`）；**改 `root` 需要的联动见第 1.1 节第 1 项说明** |
| 改导航菜单 / 头像 / 社交链接 | `themes/butterfly/_config.yml`（`menu:` / `avatar:` / `social:`） |
| 挂载 / 卸载自定义 CSS / JS | `themes/butterfly/_config.yml` → `inject:` 段 |
| 改主题色 | `source/css/variables.css`（站点自定义色板）+ `themes/butterfly/_config.yml` 的 `theme_color:` 段（Butterfly 自己的主题色，两处要一起看） |
| 改首页文章卡片（样式） | `source/css/post-list.css` |
| 改首页文章卡片（结构 / 元信息 / 默认封面 / 置顶图标与文字） | `themes/butterfly/layout/includes/mixins/indexPostUI.pug` |
| 改画廊页（瀑布流样式） | `source/css/gallery.css`（页面容器的宽度/去底板在 `source/css/layout-fix.css` 的 `.page.type-gallery` 段） |
| 加画廊图片 | 图丢进 `source/carousel/`，重新构建即可（自动扫描） |
| 改画廊渲染 / 随机顺序 / 灯箱 | `source/js/gallery.js` |
| 改首页活跃度热力图 | `source/js/activity.js` + `source/css/activity.css` |
| 改活跃度数据统计口径 | `scripts/activity.js`（构建时执行 `git log --all` 按天计提交数；失败时回退为按文章日期） |
| 改欢迎动画 / 台词 | `source/css/welcome_animation.css` + `source/js/welcome_animation.js`（台词在文件顶部的 `quotes` 数组） |
| 改页首封面图 | `themes/butterfly/_config.yml` 的 `index_img` / `default_top_img`（当前都是 `/img/covers/enterprise_2.jpg`） |
| 改页首视频 / 手机端页首封面 | `source/videos/cover.mp4`、`source/img/phone_cover.jpg`、`source/js/video-cover.js` |
| 改首页磁贴（学习 / 热爱） | `source/js/carousel.js` 里的 `buildTiles()`（图 `img/study.jpg`、`img/love.jpg`）+ `source/css/tiles.css` |
| 改 BGM | `source/js/music.js`（音频在 `source/music/bgm.mp3`） |
| 改音乐页歌单 | `source/music/index.md`（`<meting-js>` 的 `id`） |
| 改设置面板（暗色 / 背景 / 模糊 / 卡片透明度 / 页面动画 / BGM 开关） | `source/js/settings-menu.js` + `source/css/settings-menu.css`；首屏前应用设置的内联脚本在 `themes/butterfly/_config.yml` 的 `inject.head` 第一条（约定见第 5.4 节） |
| 改侧边栏天气卡片 | `source/js/weather.js` + `source/_data/widget.yml` + `source/css/weather.css` |
| 改首页文章排序 / 置顶 | `source/js/random_posts.js`（置顶依据是文章 front matter 的 `sticky`，经 `indexPostUI.pug` 渲染成 `i.sticky`；已没有 `PINNED` 数组） |
| 改阅读模式（侧栏只留目录） | `source/css/reading-mode.css`（样式）+ `source/js/toc-check.js`（有无目录时侧栏卡片的显示） |
| 改日志输出（浏览器端） | `source/js/logger.js`（DSLog 工具，见第 8.1 节） |
| 改构建 / 本地日志 | `scripts/daily-log.js`（见第 8.2 节） |
| 改站点路径前缀（`/azur_blog/`）的派生方式 | `source/js/blog-config.js`（`BlogConfig.url()`，前端脚本统一从这里取路径） |
| 改 PJAX 页面切换后的功能重新挂载方式 | `source/js/lifecycle.js`（`BlogLifecycle`，见第 9 节，新增动态 DOM 的功能都应该接入这个而不是自己监听 `pjax:complete`） |
| 让某个页面不走 PJAX（整页刷新） | `themes/butterfly/_config.yml` 的 `pjax.exclude`（目前只有 `/music/`） |
| 改公式渲染方式 | `_config.yml` 的 `mathjax:` 段 + `themes/butterfly/_config.yml` 的 `math:` / `katex:` 段（见第 5.3 节） |
| 文章里的公式写法 | 直接写 LaTeX：行内 `$...$`、块级 `$$...$$`（无需 front matter，已全站启用） |
| 换头像 | 覆盖 `source/img/avatar.png` |
| 改新建文章时的默认格式 | `scaffolds/post.md`（目前只有 `title` / `date` / `tags`，**没有 `categories`**，见 4.2） |
| 写 / 改 / 删文章 | `source/_posts/` 下的 `.md` 文件 |
| 改部署流程 | `auto-deploy.cmd`（见第 11 节） |
| 改构建校验 | `test/validate-build.js`（见第 1.1 节） |

> **关于站点标题**：`_config.yml` 的 `title: ''` 是**有意留空**的（站名靠 subtitle 与页首视觉呈现，不用 h1 文字）。实测的副作用：
> - 首页 `<title>` 输出成「 - 欢迎来到灰色幽灵的博客 -」——开头多一个「 - 」，因为 Butterfly 的 `head.pug` 拼副标题时（搜索 `isSubtitle`）固定加了 `' - '` 前缀。若想去掉，改 `_config.yml` 的 `subtitle`（删掉它开头的空格与结尾的「 - 」），不要动 `title`。
> - 文章页 `<title>` 输出成「文章标题 | 」（末尾多一个分隔符），因为 `head.pug` 里是 `pageTitle + ' | ' + config.title`，而 `title` 为空。

---

## 4. 文章怎么写

### 4.1 新建文章

```bash
npx hexo new "文章标题"
```

### 4.2 front matter 约定

```markdown
---
title: 文章标题
date: 2026-08-12 00:00:00
categories: 技术          # 单个分类；自动生成 /categories/技术/ 页面
tags: [js, hexo]          # 可多个
cover: /img/xxx.jpg       # 可选：首页卡片封面图（不加则使用默认封面 /img/文章封面/if.jpg）
sticky: 1                 # 可选：置顶（数字越大越靠前）
---
```

- **`scaffolds/post.md` 目前只生成 `title` / `date` / `tags`**，新建文章后要**手动补 `categories`**，否则文章不会出现在分类页，`npm test` 也会给出警告（目前 `博客制作相关/我的第一篇博客.md` 就缺这一项）。
- **分类**：通常选择“学习”或“热爱”（首页两块磁贴分别链到 `categories/学习/`、`categories/热爱/`）。
- **封面**：不写 `cover` 的卡片显示默认封面，图片位置在 `source/img/文章封面/if.jpg`；这个路径**写死在 `indexPostUI.pug`**（搜索 `if.jpg`），主题配置里的 `cover.default_cover` 是空的、没有在用。
- 文章里放图片：图片放 `source/img/`，用 `/img/xxx.png` 引用（注意用半角 `![...](...)`，全角标点会导致图片不渲染）。
- 首页每页显示 6 篇（`_config.yml` 的 `index_generator.per_page`）；`random_posts.js` 只会在**当前页**的卡片内洗牌。

### 4.3 文章链接是怎么生成的

`_config.yml` 里 `permalink: :year/:month/:day/:title/`，而 Hexo 的 `:title` 是**相对 `source/_posts/` 的文件路径**。实测链接形如：

```
/azur_blog/2026/08/23/408/计算机网络/王道计算机网络第一章笔记/
```

也就是说：**子目录名会进入文章链接，把文章挪到别的子目录会改变它的链接**（外部引用、搜索引擎收录的旧链接随之失效）。

---

## 5. 自定义功能是怎么挂载的

所有自定义 CSS / JS 都通过主题配置的 `inject:` 段挂载（`themes/butterfly/_config.yml`）。**CSS 放 `head`，JS 放 `bottom`（按顺序加载）**。

### 5.1 自定义脚本（`source/js/`）

`inject.bottom` 的**实际加载顺序**：

1. `logger.js`　2. `blog-config.js`　3. `lifecycle.js`　4. `settings-menu.js`　5. `toc-check.js`　6. `gallery.js`　7. `welcome_animation.js`　8. `weather.js`　9. `carousel.js`　10. `activity.js`　11. `random_posts.js`　12. APlayer 库（jsdelivr）　13. 内联 `window.meting_api = …`　14. Meting 库（jsdelivr）　15. `video-cover.js`　16. `music.js`

硬性要求只有两条：**1–3 必须是前三个**；**`music.js` 必须在 APlayer 库之后**（它靠 `typeof APlayer` 判断库是否已加载）。其余模块的先后顺序 = `BlogLifecycle` 的挂载顺序，会影响它们往首页插入 DOM 的先后。

| 文件 | 作用 |
|---|---|
| `logger.js` | **必须第一个加载**。统一日志工具 `DSLog`（debug/info 默认隐藏，`DSLog.enableDebug()` 或 URL 加 `?debug` 开启；**没有页面内调试面板**，详见第 8.1 节） |
| `blog-config.js` | **必须第二个加载**。统一站点路径配置 `BlogConfig`，把 Hexo 输出的 `GLOBAL_CONFIG.root`（由主题的 `head/config.pug` 输出）派生成 `BlogConfig.url(p)`，替代各文件里手写 `/azur_blog/` 前缀；`GLOBAL_CONFIG` 缺失时才回退到字面量 |
| `lifecycle.js` | **必须第三个加载**。统一 PJAX 生命周期管理器 `BlogLifecycle`，提供 `register(name, {mount, destroy, refresh, onSend, persistent})`，自动回收模块内部的定时器/监听器/Observer/rAF/DOM 节点，详见第 9 节 |
| `settings-menu.js` | 设置面板（导航栏齿轮按钮 → 右侧抽屉）：暗色模式、背景切换、背景模糊、卡片透明度（低 / 高 / 自定义滑块）、页面动画、BGM 开关、重置。`persistent: true` 模块，约定见第 5.4 节 |
| `toc-check.js` | 目录检查（文章页有目录时侧栏只显示目录；无目录时恢复所有卡片，并给 `<html>` 加/去 `no-toc` 类）。已接入 `BlogLifecycle` |
| `gallery.js` | 画廊页瀑布流（读取 `gallery.json`，随机打乱后逐张放进当前最矮的一列，一次性渲染全部图片，无分页；灯箱走 Butterfly 的 `btf.loadLightbox`，主题配置 `lightbox: fancybox`）。已接入 `BlogLifecycle`，resize 防抖走 `ctx.on`/`ctx.timeout` |
| `welcome_animation.js` | 首页欢迎动画（灰色幽灵 + 打字机台词）。已接入 `BlogLifecycle`，定时器链走 `ctx.timeout`。**动画被关闭时**（设置面板关掉「页面动画」，或系统开启「减少动态效果」）：立刻显示完整台词，之后每 8 秒直接换一句（不打字）；重新开启则马上恢复打字 |
| `weather.js` | 侧边栏天气卡片（固定郑州，数据来自 `wttr.in`，30 分钟缓存）。已接入 `BlogLifecycle`（`persistent: true`，数据缓存与 UI 渲染分离，PJAX 后只重绘不重新请求） |
| `carousel.js` | **只有首页磁贴（学习 / 热爱）在实际工作**。文件里的「画廊页轮播」分支要求页面里有 `#gallery-carousel`，而 `source/gallery/index.md` 里没有这个容器，所以这个分支**目前不会执行**（连带 `scripts/carousel.js`、`carousel-list.json`、`carousel.css` 也只是被生成/加载，没有页面在用），见第 13 节。已接入 `BlogLifecycle` |
| `activity.js` | 首页活跃度热力图（**Git 提交记录版**）：按自然月季度切成四块横排（1–3月、4–6月、7–9月、10–12月），统计起点取 `data/activity.json` 里最早的一天，「最近一周 / 一月」= 含今天的最近 7 / 30 天。**仅在视口 ≥1025px 时请求数据并渲染**（与 `activity.css` 里 ≤1024px 隐藏的断点一致）。已接入 `BlogLifecycle` |
| `random_posts.js` | 首页文章随机排序（置顶文章保持在最前，其余在**当前页内**随机），置顶判断读取构建期渲染的 `i.sticky`，不按标题字符串匹配。**未接入 `BlogLifecycle`**，自己监听 `pjax:complete` |
| `video-cover.js` | 页首视频封面（≤768px 不挂视频，改用 `phone_cover.jpg` 竖图）。已接入 `BlogLifecycle` |
| `music.js` | BGM 播放器（APlayer 1.x，注意其 `play()/pause()` 不返回 Promise，且内部已吞掉「自动播放被拦截」的错误，所以判断是否在播只能看 `audio.paused`）。被浏览器拦截自动播放时，会在第一次用户手势（pointerdown/pointerup/touchend/keydown/click）后再播，成功后移除全部监听。已接入 `BlogLifecycle`（`persistent: true`，全局只创建一次实例，PJAX 后只做健康检查+UI 同步，不重建） |

> **迁移状态**：除 `logger.js` / `blog-config.js` / `lifecycle.js`（基础设施）和 `random_posts.js`（未接入）外，其余业务模块都通过 `BlogLifecycle.register()` 管理。**注意「全站只有一个 `pjax:complete` 监听」并不成立**：正常情况下（`BlogLifecycle` 存在）自定义脚本里监听它的有 `lifecycle.js` 和 `random_posts.js` 两处；其余模块里的 `pjax:complete` 监听只写在「`BlogLifecycle` 缺失」的兜底分支里，平时不会挂上；此外 Butterfly 自己的 `pjax.pug` 里也有监听（见第 9 节）。

### 5.2 自定义样式（`source/css/`）

`inject.head` 的加载顺序：内联设置脚本 → 字体（fonts.loli.net）→ `variables.css` → `layout-fix.css` → `weather.css` → `carousel.css` → `tiles.css` → `activity.css` → `post-list.css` → `sidebar.css` → `reading-mode.css` → `welcome_animation.css` → `settings-menu.css` → `custom.css` → APlayer 样式（jsdelivr）→ `music-page.css` → `gallery.css`。`variables.css` 必须排在本地样式最前（其余文件都引用它定义的变量）。

| 文件 | 作用 |
|---|---|
| `variables.css` | **色板与变量**（改颜色只改这里）：`--navy-deep` / `--navy-mid` / `--gold` 等，以及卡片背景 `--card-bg-light` / `--card-bg-dark`、圆角、字体变量 |
| `layout-fix.css` | 全局布局微调 + 画廊页特殊样式（`.page.type-gallery`） |
| `post-list.css` | 首页文章卡片（竖版 2 列网格） |
| `gallery.css` | 画廊瀑布流样式 |
| `activity.css` | 活跃度热力图样式（≤1024px 隐藏） |
| `sidebar.css` | 侧栏卡片样式（暗色背景读取 `--card-bg-aside-dark`，缺省 `#28282d`） |
| `reading-mode.css` | 阅读模式（文章页侧栏只留目录） |
| `settings-menu.css` | 设置面板样式 + 卡片透明度规则（`data-card-opacity`）+ 滑块样式 + 键盘焦点样式 |
| `carousel.css` / `tiles.css` / `weather.css` / `welcome_animation.css` / `music-page.css` | 对应组件样式 |
| `custom.css` | **空占位**（内容只有一行注释「此文件已被解耦」），仍被 `inject.head` 引用，可以从 inject 里去掉 |

要新增脚本/样式：文件放进 `source/js/` 或 `source/css/`，在 `inject:` 段加一行，**并记得升级版本号（见第 6 节）**。

### 5.3 公式渲染（MathJax 服务端 SVG）

- **方案**：`hexo-filter-mathjax` 在**构建期**用 MathJax 4 把 LaTeX 渲染成**内联 SVG**（矢量图）——无前端 JS、无字体文件依赖，页面加载即完整显示。
- **渲染器**：`hexo-renderer-markdown-it`（**纯** markdown-it）。
  ⚠️ **不要再装 `hexo-renderer-markdown-it-katex`**：它内置 KaTeX 插件抢渲染，且硬编码注入 `cdn.bootcss.com` 的 KaTeX CSS——该 CDN 已停服，会导致公式符号空白（详见第 13 节已解决记录）。
  ⚠️ 目前 `package.json` 里**还残留一个 `@renbaoshuo/markdown-it-katex` 依赖**（没有被启用，`markdown.plugins` 为空），与上面的原则冲突，建议卸载（见第 13 节）。
- **配置**：
  - `_config.yml` → `mathjax:`：`every_page: true`（全站启用，无需逐篇 front matter）、`append_css: true`（注入本地 CSS）、`single_dollars: true`、`packages: [ams]`
  - `themes/butterfly/_config.yml` → `math.use` 留空、`katex.enable: false`（**关闭主题前端数学渲染**，避免与 MathJax 冲突）
  - `_config.yml` → `markdown.plugins` 留空（不挂 katex 插件）
  - `_config.yml` → `markdown.render` 里 `breaks: true`（单个换行变 `<br>`）、`typographer: true`（自动转换引号/破折号）是全站生效的
- **写法**：行内 `$F_1(x)$`、块级 `$$...$$` 即可，不必加 `mathjax: true`。
- **副作用**：构建变慢属正常（服务端渲染较重），公式密集的文章尤其明显；产出 HTML 体积略增（内联 SVG）。

### 5.4 设置面板与主题变量的约定（跨文件，改之前先读）

设置面板的状态分散在四个地方，**改其中任何一处都要检查其余三处**：

| 位置 | 内容 |
|---|---|
| `localStorage` 键 `azur-blog-settings` | JSON：`blur`（布尔）、`animation`（布尔）、`cardOpacity`（`'low'` / `'high'` / `'custom'`）、`cardAlpha`（数字，0.4–1，仅 `custom` 时生效）。读到旧值 `'default'` 或任何无效值都按 `'low'` 处理 |
| `localStorage` 键 `azur-blog-background` | 自定义背景的路径（`BlogConfig.url('img/…')` 的结果，以 `/` 开头）。暗色模式沿用 Butterfly 自己的 `theme` 键，面板「重置」时会一并删除 |
| `<html>` 与 `<head>` | 属性 `data-card-opacity`、行内变量 `--card-alpha`、类 `settings-disable-animation`、`<style id="azur-bg-override">`（内容是 `#web_bg{background-image:url("…")!important}`）、`#web_bg` 上的类 `settings-blur` |
| 首屏内联脚本 | `themes/butterfly/_config.yml` 的 `inject.head` **第一条**：在页面首次绘制前读取上面两个键并设置 `<html>` 属性/类和背景 `<style>`，避免加载时闪一下默认样式。它是内联的，**没有版本号**；**改键名、数值范围（`0.4`–`1`）或 `<style>` 的 id 时，必须同时改 `settings-menu.js` 和这段脚本** |

卡片透明度的三档在 CSS 里的含义（`settings-menu.css`）：

- **低（默认）**：没有 `data-card-opacity`、或值不是 `high` / `custom`，都落在这一档。Butterfly 自带卡片的 `--card-bg` 为 0.92；站点自定义卡片**不覆盖**，沿用 `variables.css` 的原始值（浅色 0.92 / 暗色 0.7 / 侧栏暗色 `#28282d`）。
- **高**：Butterfly 卡片 0.65；自定义卡片浅色 0.65、暗色 0.5。
- **自定义**：所有卡片统一用 `--card-alpha`；`var(--card-alpha, 0.92)` 的回退值不能删（变量缺失时没有回退会让 `rgba()` 整条失效，卡片背景消失）。
- 滑块显示的是「透明度 %」= (1 − alpha) × 100，范围 0–60%；预设「低 / 高」对应滑块的 8% / 35%。

动画开关：`html.settings-disable-animation` 会用 `!important` 把页面里所有 CSS 动画/过渡压到近乎 0。**JS 驱动的动画管不到**——需要自己判断 `motionReduced()`（`welcome_animation.js` 里同时检查这个类和 `prefers-reduced-motion`）。

---

## 6. 资源版本号规则（非常重要）

`inject:` 里每个**本地**资源都带 `?v=x.y` 版本号，**每个文件有自己独立的版本号**（当前从 1.0 到 1.7 不等，不是统一的 1.2）。**每次修改某个自定义 CSS/JS 后，必须把它自己那一项的版本号 +1**（例如 `settings-menu.js?v=1.3` → `?v=1.4`），否则浏览器用缓存、改动不生效。

> 这个坑已经踩过四次（WebP 不显示、设置菜单消失、手机封面不生效等），都是版本号没升。**改完文件 → 升版本号 → 重新构建**，三步缺一不可。
>
> `npm test` 只检查「`?v=` 存在与否」（缺了是**警告**，不阻止部署），**发现不了「改了文件却忘了升版本号」**——这一步只能靠自觉。首屏内联脚本没有版本号（它内联在每个页面里，随页面更新）。

---

## 7. Git 规则

- `.gitignore` 目前覆盖：`node_modules/`、`public/`、`.deploy_git/`、`db.json`、`*.log`、`.DS_Store`、`Thumbs.db`、`.vscode/`、`.idea/`、`*.tmp`、`*.temp`、`source/_img_backup/`、`source/carousel/`、`daily_log/`、`.env`、`.env.*`、`.claude/`、`指令.txt`、`.netlify`。**不要**手动提交这些。
- **`.gitignore` 的缺口**：`cache_tmp/`、`true/`、`_update-notifier-last-checked` 没有被忽略，已经被误提交进仓库（多半是 `auto-deploy.cmd` 的 `git add .` 顺手带上的）。清理办法见第 13 节。
- **手动提交时**：每次改动做成小提交，`git add` 只加本次涉及的文件，不要 `git add .`。提交信息用中文写清「做了什么 + 为什么」。
- **例外**：`auto-deploy.cmd` 会执行 `git add .` + 自动提交。**脚本启动时会询问提交信息**：直接回车则用默认的 `Auto deploy: 日期 时间`，输入内容则用你写的信息。所以想让提交历史有意义，在部署时输入即可，不必改脚本。日常开发仍建议手动提交，只在发布时跑 auto-deploy。脚本会先用 `git diff --cached --quiet` 判断有没有真正的改动，没有改动就跳过 commit，但**仍然会尝试 push**（避免上次 commit 成功但 push 失败、遗留在本地的提交被无限期搁置）。因为用的是 `git add .`，**`.gitignore` 漏掉的杂物都会被提交**。
- `themes/butterfly/` 以 vendor 方式被 git 跟踪。**我们相对上游 5.7.0 改过的文件一共 7 个**（比对方法：`npm pack hexo-theme-butterfly@5.7.0` 解包后与 `themes/butterfly/` 对比，忽略行尾差异）——升级主题前注意备份这些改动：

| 文件 | 改了什么 |
|---|---|
| `themes/butterfly/_config.yml` | 菜单、头像、封面、`theme_color`、搜索、`lightbox`、`pjax.exclude`、`inject`、`aplayerInject` 等站点定制 |
| `themes/butterfly/layout/includes/mixins/indexPostUI.pug` | 首页文章卡片改为竖版结构；无 `cover` 时用默认封面；置顶标记（图标 + 文字标签） |
| `themes/butterfly/layout/includes/pagination.pug` | 分页链接格式去掉 `#content-inner` 锚点（修 PJAX 分页边界问题） |
| `themes/butterfly/layout/includes/third-party/pjax.pug` | PJAX 初始化加 `scrollTo: false`，并在 `pjax:complete` 里手动滚动到 `#content-inner` |
| `themes/butterfly/layout/includes/widget/card_announcement.pug` | 公告卡片标题图标由喇叭改为星星 |
| `themes/butterfly/languages/zh-CN.yml` | `card_announcement` 由「公告」改为「理念」 |
| `themes/butterfly/source/js/main.js` | 仅多了两个空行（无实质改动） |

---

## 8. 日志与排查

### 8.1 浏览器端（DSLog）

- 所有自定义脚本用 `DSLog.info/warn/error/debug('模块', '消息', 数据)` 输出统一格式日志。
- **打印规则**：`warn`/`error` 始终打印到控制台；`debug`/`info` 只在调试模式下打印。**所有级别都会写入缓冲区**（内存最近 500 条；每 5 秒或累计 20 条批量写入 `localStorage` 的 `blog_log_buffer`，保留最近 200 条）——这意味着**每个访客的浏览器里都会有这份日志缓存**。
- **开启调试**：控制台执行 `DSLog.enableDebug()`，或地址栏加 `?debug`，或 `localStorage` 里存 `blog_debug=1`；`DSLog.disableDebug()` 关闭；`DSLog.isDebug()` 查询。
- **导出 / 清空**：`DSLog.export()` 下载 `YYYY-MM-DD-前端.log`；`DSLog.getBufferSize()` 查看条数；`DSLog.clearBuffer()` 清空。手机浏览器上没有控制台时，可以用 `?debug` 打开后执行导出，或在电脑上远程调试。**目前没有页面内的悬浮调试面板**（早期文档写过 🐞 面板，但 `logger.js` 里并没有这部分代码）。
- 全局错误捕获：未捕获 JS 错误和 Promise 拒绝会自动上报（模块 `global`）。
- 脱敏：日志内容里的用户目录名（`/home/…`、`/Users/…`、`C:\Users\…`）和 20 位以上的字母数字串会被替换。

### 8.2 构建端（daily_log）

- `scripts/daily-log.js` 在每次构建/部署时自动写入 `daily_log/YYYY-MM-DD.log`，**自动清理 7 天前的日志**，git 忽略。
- `npm run build` / `npm run deploy`（含 `auto-deploy.cmd` 里跑的那些）只记录「构建开始 / 构建完成 / 部署开始 / 部署完成」这类极简条目。
- **`npm run server` 本地会话下会额外记录**：本次构建耗时、生成的文件数与文章数、由哪个文件改动触发的增量构建（如 `改动: _posts/xxx.md`）、以及构建过程中真正的 `[WARN]`/`[ERROR]` 内容（挂在 `hexo.log` 上，不影响控制台原有输出）。
  > 已知小瑕疵（文档记录，未验证）：文件改动到「归因到日志里哪一行」偶尔会错位一格，不影响构建本身。
- 排查构建问题：看 `daily_log/` 当天文件；本地开发时报错信息会直接出现在这份日志里，不用再翻终端滚动的输出。
- `lib/node-logger.js` 设计上会写另一份 `YYYY-MM-DD-后端.log`，但目前没有代码引用它，所以这类日志实际不存在。

---

## 9. PJAX 注意事项（容易踩坑）

博客开启了 PJAX（`themes/butterfly/_config.yml` 的 `pjax.enable: true`，页面无刷新切换），`#body-wrap` 会被整体替换。**`/music/` 页面被排除在 PJAX 之外**（`pjax.exclude`），进出该页面是整页刷新。**凡是往页面里动态注入 DOM 的自定义脚本，都必须在 PJAX 完成后重新执行**，否则切换页面后功能消失。

**标准做法：接入 `BlogLifecycle`**（`source/js/lifecycle.js`，见第 5.1 节）。写法：

```js
window.BlogLifecycle.register('模块名', {
  mount: function (ctx) {
    // 首次挂载 / PJAX 后重新挂载
    // 用 ctx.on / ctx.interval / ctx.timeout / ctx.raf / ctx.observer / ctx.append 申请资源
    // 会在销毁时自动回收，不用自己手写清理逻辑
  },
  destroy: function (ctx) { /* 可选：处理第三方实例等 ctx 管不到的东西 */ },
  refresh: function (ctx) { /* 可选：persistent 模块在 PJAX 后更新页面相关部分 */ },
  persistent: false  // 全局常驻组件（如播放器、天气卡片、设置面板）设 true，PJAX 后只 refresh 不销毁重建
});
```

关键保证：`mount()` 一定在 `destroy()`（或 `refresh()`）之后才可能再次执行；单个模块抛错不会影响其他模块。`ctx.timeout` / `ctx.raf` 触发后会自动从登记表移除，递归定时器（如打字机）不会让登记表无限增长。

目前用这条路的有：`toc-check.js`、`gallery.js`、`welcome_animation.js`、`weather.js`、`carousel.js`、`activity.js`、`video-cover.js`、`music.js`、`settings-menu.js`。新增任何涉及动态 DOM 的功能都应该走这条路，不要再自己单独写：

```js
document.addEventListener('pjax:complete', init); // 旧写法，不再新增（random_posts.js 是遗留的例外）
```

另外，Butterfly 自己的 `pjax.pug` 也监听 `pjax:complete`：因为我们改成了 `scrollTo: false`，切换后由它手动把页面滚动到 `#content-inner`（找不到则回到顶部）。

已知易错点：PJAX 后元素是**新节点**（旧引用失效），事件要用动态查询；容器渲染前先 `innerHTML = ''` 防叠加。

> **踩过的坑**：`lifecycle.js` 和 `blog-config.js` 曾经只是写好了代码放在 `source/js/` 里，但**忘了把它们加进 `themes/butterfly/_config.yml` 的 `inject.bottom` 列表**，导致 `window.BlogLifecycle` / `window.BlogConfig` 在线上一直是 `undefined`，所有引用它们的模块全部悄悄走了兜底分支，新架构形同虚设，且没有任何报错提示。**新增 `source/js/` 文件后，一定要去 `inject.bottom` 里加对应的 `<script>` 标签，并跑 `npm run test:build` 确认「inject 引用的资源确实存在于构建产物里」这一项检查通过**（第 1.1 节第 4 项）。

---

## 10. 给 AI 协作者的快速指引

- 先读本文档再动手，不要凭猜测改文件。
- **只改该改的文件**：站点配置 `_config.yml`，主题配置 `themes/butterfly/_config.yml`，文章 `source/_posts/`，自定义样式脚本 `source/css` / `source/js`。
- 分类用 front matter 的 `categories`，不要在 `source/` 下新建分类目录。
- 改完任何自定义 CSS/JS：**升级 `inject:` 里那个文件自己的版本号 +1**，提醒用户强刷（Ctrl+Shift+R）。
- 涉及动态注入 DOM 的功能：**优先用 `BlogLifecycle.register()`**（见第 9 节），不要再自己单独监听 `pjax:complete`。
- 新增 `source/js/` 或 `source/css/` 文件后：**必须**去 `themes/butterfly/_config.yml` 的 `inject:` 段加对应的 `<script>`/`<link>` 标签，写完代码不代表它会被加载——这个坑已经真实踩过一次（见第 9 节末尾），加完后跑 `npm run test:build` 确认校验通过。
- 改设置面板相关的任何一处（`settings-menu.js` / `settings-menu.css` / `sidebar.css` / `variables.css` / `inject.head` 的内联脚本），先读第 5.4 节。
- 改配置后提醒用户跑 `npm run clean && npm run build` 验证。
- 不要改 `public/`、`db.json`、`node_modules/`、`daily_log/`、`source/_img_backup/`。
- 需要「去 AI 味」时，可用 `kill-ai-slop` 技能，先扫描报告、用户确认后再动手。
- 除非特殊说明或者经过明确确认，否则不要加毛玻璃。
- 处理 `需要解决的问题.md` 里的条目前，先核实是否已经修复（见第 2 节末尾说明）；确认修复后，把该条目改成「✅ 已解决」并简述修复方式（格式参考本文档第 13 节「已解决（历史记录，勿再改回）」），**不要直接删除条目**——保留记录能防止以后又被同一个问题绕进去。反过来，发现新的技术债，也应该补充进 `需要解决的问题.md` 而不是只在对话里提一句就过去。
- 修改 `scripts/` 目录下任意文件后（`activity.js` / `carousel.js` / `daily-log.js` / `gallery-list.js` / `netlify-redirects.js`）：**必须**跑一次 `npm run test:build`，确认生成的 JSON 结构仍能通过 `test/validate-build.js` 的校验（第 1.1 节）——这些脚本参与构建期数据生成，JS 不报错不代表数据结构没变，前端可能悄悄失效。
- **写文档时不要引用行号**，用文件名或搜索关键词。改了行为要同步本文（第 14 节）。

---

## 11. 部署说明与排障

### 双仓库机制（务必理解）

- **`main` 分支**：源码仓库（md/脚本/配置，**不含画廊图**——`source/carousel/` 已 gitignore，图只在本机）
- **`gh-pages` 分支**：网页仓库（`npm run deploy` 推送的 `public/` 构建产物，含压缩后的图）。`_config.yml` 的 `deploy` 段：`type: git`、`repo: https://github.com/11408-prog/azur_blog`、`branch: gh-pages`、`force: true`
- GitHub Pages **只显示 gh-pages**；main 里有没有图与网页无关

### 部署流程（auto-deploy.cmd 一键完成）

脚本**启动时先询问两件事**：① 是否同时部署 Netlify（输入 `1` 才部署，默认不部署）；② 提交信息（留空则用 `Auto deploy: 日期 时间`）。

之后**实际执行顺序**（以 `auto-deploy.cmd` 为准；脚本内部标号是 `[1/6]`–`[6/6]`，可选的 Netlify 不在其中）：

1. `hexo clean`（清理 `public/` 与 `db.json`；失败则中断）
2. `hexo generate -c 4`（4 并发构建；失败则自动降级 `hexo generate -c 1` 串行重试；仍失败则中断）
3. `node test\validate-build.js`（构建产物校验，见第 1.1 节；不带 `--strict`，**只有「错误」会中断，「警告」不会**；找不到校验脚本则只警告并跳过）
4. `hexo deploy`（把 `public/` 强推 gh-pages，更新 GitHub Pages；失败仅警告，不中断）
5. `git add .` + （有改动才）`git commit` + `git push origin main`（推源码；commit 没有改动会跳过，但只要 commit 没失败就总会尝试 push；失败仅警告）
6. `wrangler pages deploy ./public --project-name=azurlane-enterprise --branch=main`（部署 Cloudflare Pages；找不到 wrangler 或部署失败**仅警告，不中断**）
7. 可选：`netlify deploy --dir=public --prod`（仅当启动时选了部署 Netlify；找不到 netlify-cli 或失败**仅警告，不中断**）

> 注意顺序是**先构建部署产物、后推源码**，与直觉相反但无碍：两者内容相互独立。  
> **第 4、5、6、7 步全部是失败仅警告、不中断**——脚本"跑到底"不代表每一步都成功。结尾会打印一份汇总：`GitHub Pages` / `GitHub Source` / `Cloudflare Pages` 各自是 `SUCCESS` 或 `FAILED`，`Netlify` 是 `SUCCESS` / `FAILED` / `SKIPPED`（没找到 CLI）/ `NOT REQUESTED`（启动时没选），另外会打印总提交数。**看这份汇总，而不是看脚本有没有报错退出**——脚本走到结尾时总是 `exit /b 0`。  
> 只有第 1、2、3 步（清理/构建/校验错误）失败才会真正中断脚本（此时退出码为 1）——这几步失败意味着连能部署的产物都没有，中断是合理的。

### 为什么不用 CI

画廊图只在本机，云端 clone 不到 `source/carousel/`，**自动构建会得到空画廊并覆盖线上**。
CI（deploy.yml）已删除，`.github/` 下现在只有 `dependabot.yml`；只保留本地部署。

### 排障：线上画廊空 / 页面显示源码（踩过的大坑）

- **症状**：线上 `gallery.json` 是 `[]`，或页面内容看起来是源码而非构建产物
- **根因**：`.deploy_git` 目录的 `.git` 被删后，`hexo deploy` 找不到部署仓库的 git，会**向上误用主仓库的 `.git`**，把 **main 源码**强推到 gh-pages（未验证：来自历史事故记录）
- **修复**：删除**整个** `.deploy_git` 目录，重新 `npm run deploy`（hexo 会完整重建）
- **教训**：清部署历史必须删**整个 `.deploy_git`**，让 hexo 重建；只删 `.git` 会出大问题

### 排障：推送超时（HTTP 408）

- 原因：仓库体积大（图进 git / gh-pages 历史累积）。**现在 `cache_tmp/` 的 26MB npm 缓存也在仓库里，是新增的体积来源**
- 已配置：`git config http.postBuffer 524288000`（500MB，跟仓库走）（未验证：本机 git 配置，不在源码里）
- 已处理：图 gitignore（main）+ gh-pages 历史清空（force 单提交）
- **不要设 `http.timeout`**（会掐断慢推送）；postBuffer 500MB 已足够

---

## 12. 外部依赖清单（挂了会影响什么）

站点有若干功能依赖第三方服务，**它们不受本仓库控制**，失效时表现为"某个功能突然坏了"。排查前先对照下表，别去翻自己的代码。

| 依赖 | 用途 | 引用位置 | 失效表现 |
|---|---|---|---|
| `fonts.loli.net` | Noto Serif SC 中文字体 | `inject.head` 里的字体 `<link>` | 字体回退为系统默认，版式略变，**不影响内容** |
| `cdn.jsdelivr.net`（站点自己引用） | APlayer 1.10.1（CSS + JS）、Meting 2（JS） | `inject.head` / `inject.bottom` | 首页 BGM 与音乐页**均无法播放** |
| `cdn.jsdelivr.net`（主题引用） | 首页 HTML 里实际出现：Font Awesome 7.3.1、Fancybox 6.1.14（CSS + JS）、vanilla-lazyload 19.1.3、pjax 0.2.8、@egjs/infinitegrid 4.13.0 | 主题配置 `CDN.third_party_provider: jsdelivr`（清单见 `themes/butterfly/plugins.yml`） | 以下为推断，均未在故障状态下实测：图标丢失、灯箱不可用、PJAX 不可用（页面退化为整页跳转）；**所有页面的 `<img>` 都是 `data-lazy-src` + 1×1 占位图（`lazyload: true`），由 lazyload 库替换真图，该库加载失败时图片可能一直停在占位图上** |
| `api.injahow.cn` | Meting 歌单解析接口（第三方公共服务） | `inject.bottom` 内联 `window.meting_api` | **音乐页 `/music/` 歌单空**（网易云歌单 id `18308336739`）；首页 BGM 不受影响 |
| `wttr.in` | 侧栏天气数据（固定郑州） | `source/js/weather.js` 的 `API_URL`（搜索 `wttr.in`） | 天气卡片无数据/显示异常 |
| GitHub Pages | 站点主访问入口 | `_config.yml` `deploy` | 主链接打不开；Netlify/Cloudflare 镜像仍可用 |
| Cloudflare Pages | 站点镜像 | `auto-deploy.cmd` 第 6 步（需要 `wrangler login`） | 镜像失效；GitHub Pages 仍可用 |
| Netlify | 站点镜像 | `auto-deploy.cmd` 可选步骤 | 镜像失效；GitHub Pages 仍可用 |

要点：

- **首页 BGM 的音频走本地文件**（`source/music/bgm.mp3`，由 `source/js/music.js` 加载），不依赖 Meting 接口——所以"音乐页坏了但首页还有声音"是正常现象，不是 bug。但**播放器库 APlayer 本身来自 jsdelivr**，jsdelivr 不可用时首页 BGM 也会消失（`music.js` 检测到 `APlayer` 缺失会记一条错误日志并跳过初始化）。
- **公式不依赖任何 CDN**：历史上 KaTeX 走 BootCDN 导致符号空白，现已改为 MathJax 服务端渲染（构建期出 SVG），彻底摆脱外部字体/CDN（见第 5.3 节）。
- 天气与歌单都是**第三方免费接口，无 SLA**，偶发不可用属正常，不必改代码。若要长期稳定，天气可换和风/心知等带 key 的服务，歌单可改为纯本地音频列表。
- `hexo-tag-aplayer` 依赖已在 `package.json` 中保留（供 `{% aplayer %}` 标签使用）。`_config.yml` 里 `aplayer.asset_inject: false` 表示该插件不自动注入资源；主题配置里另有 `aplayerInject: enable: true, per_page: true`（Butterfly 自己的按页注入开关）；站点实际靠 `inject` 手动挂载 APlayer。

---

## 13. 已知问题 / TODO

已解决（历史记录，勿再改回）：

- [x] ~~favicon 缺失~~：`source/img/favicon.png`（真 PNG，8.2KB）。不要用 .ico 伪装 .png。
- [x] ~~URL / 部署未配置~~：`url` 与 `deploy.type: git` 均已配置，`npm run deploy` 可发布。
- [x] ~~图片格式坑~~：曾有用 iPhone HEIF 内容伪装 .jpg 导致破图，已修。加图后用 `sharp metadata` 验证真实格式；`npm test` 第 8 项会检查。
- [x] ~~_img_backup 未忽略~~：已加入 `.gitignore`。
- [x] ~~README 未完善~~：`README.md` 已重写，包含技术栈、快速开始、部署与目录结构说明。
- [x] ~~首页点文章分页「第 2 页」后再点「第 1 页」会强制刷新页面（PJAX 分页边界问题）~~：去掉 `pagination.pug` 中分页链接的 `#content-inner` 锚点，并在 `pjax.pug` 内联脚本的 `pjax:complete` 中手动滚动到 `#content-inner`（PJAX 初始化加 `scrollTo: false` 禁用自动滚动）。
- [x] ~~`check-ci.ps1` 死代码~~：已删除（2026-09-11）。它轮询 GitHub Actions 运行状态，而 CI 早已废弃，留着只会误导。
- [x] ~~`hexo-qidaink-cursor_effects` 无效依赖~~：已卸载（2026-09-11）。该插件默认 `enable: false`，且站点/主题配置里都没有 `cursor:` 段，入口直接 `return`、不注入任何东西。若将来想要光标特效，重新安装并在 `_config.yml` 加 `cursor: { enable: true, click: ..., move: ... }`。
- [x] ~~`test/` 未入库~~：已解决（2026-09-12）。从 `.gitignore` 移除 `test/`，校验脚本现在入库，换机器也能用，并集成到 `auto-deploy.cmd` 第 3 步自动运行。
- [x] ~~**公式符号不显示（数学公式渲染异常）**~~：根因是 `hexo-renderer-markdown-it-katex` 硬编码注入 `https://cdn.bootcss.com/KaTeX/0.11.1/katex.min.css`，而 **BootCDN 已停止服务** → KaTeX 样式与 20+ 个数学字体加载失败 → 符号空白。已改用 `hexo-filter-mathjax` 服务端 MathJax（**SVG 输出、零字体依赖**），并卸载 katex 渲染器、清理本地 KaTeX 资源。**以后不要再装 katex 相关渲染器/插件**（见第 5.3 节）。
- [x] ~~**两套置顶机制并存**~~：`random_posts.js` 现在读取构建期渲染的 front matter 标记（`i.sticky`）判断置顶，不再按标题字符串匹配 `PINNED` 数组，两套机制已合并为一套。
- [x] ~~**`music.js` 未带版本号**~~：已补上 `?v=` 版本号，跟其它 inject 资源一致。
- [x] ~~**`lifecycle.js` / `blog-config.js` 写了但没实际挂载**~~：两个文件曾经只是放在 `source/js/` 里，但忘了加进 `themes/butterfly/_config.yml` 的 `inject.bottom` 列表，导致 `window.BlogLifecycle` / `window.BlogConfig` 线上一直是 `undefined`，所有引用它们的模块全部悄悄走了兜底分支，且没有任何报错提示（详见第 9 节末尾的踩坑记录）。已修复：加入 `inject.bottom`，并把业务模块迁移到 `BlogLifecycle`。**教训**：新增 `source/js/` 文件后，光写代码不够，一定要去 `inject.bottom` 加挂载并跑 `npm run test:build` 验证。
- [x] ~~**齿轮菜单内联在 YAML 里**~~：已拆成 `source/js/settings-menu.js` + `source/css/settings-menu.css`（接入 `BlogLifecycle`，`persistent: true`）。`inject.head` 里现在只剩一条很短的首屏内联脚本（第 5.4 节）。
- [x] ~~**设置面板、轮播、BGM、热力图、打字机、`ctx.timeout` 的一批交互缺陷**~~：点击轮播当前圆点会锁死、BGM 自动播放兜底是死代码、设置项每次加载先闪一下默认样式、卡片透明度对首页/侧栏不生效、`localStorage` 被禁用时设置失效、手机端白白请求热力图数据、统计窗口多算一天、`ctx.timeout` 登记表无限增长、关闭页面动画后打字机停住不动，均已修复（对应行为见第 5.1、5.4 节）。

仍待处理：

- [ ] **首页置顶文章显示英文 `sticky`**：`indexPostUI.pug` 用 `_p('sticky')` 输出置顶文字标签，但 `themes/butterfly/languages/zh-CN.yml`（以及其他语言文件）里**没有 `sticky` 这个键**，所以构建产物里是 `<span class="article-meta-label">sticky</span>`（已实测）。修法：给 `zh-CN.yml` 补一个 `sticky: 置顶`（补完在本地构建后确认输出）。
- [ ] **`cache_tmp/`、`true/`、`_update-notifier-last-checked` 被误提交**：`cache_tmp/` 约 26MB、76 个文件是 npm 缓存。修法：把这三项加进 `.gitignore`，再执行 `git rm -r --cached cache_tmp true _update-notifier-last-checked`。它们也是推送体积（第 11 节的 HTTP 408 风险）的来源之一。
- [ ] **画廊页轮播是死功能**：`source/gallery/index.md` 里没有 `#gallery-carousel`，`carousel.js` 的轮播分支永远不执行；`scripts/carousel.js` 每次构建仍在扫描 `source/carousel/` 下的全部图片并生成 `carousel-list.json`，`npm test` 第 11 项也在校验它。要么在画廊页加上容器让它生效，要么删掉 `scripts/carousel.js`、`carousel.css`、`carousel.js` 里的轮播分支，以及校验里的第 11 项。
- [ ] **「活跃度」页面是空页面**：`source/activity/index.md` 只有一个没人填充的 `<div id="activity-chart">`，菜单里也没有入口；首页的热力图（`#home-heatmap`）与它无关。要么补上渲染脚本与菜单入口，要么删除该页面。
- [ ] **`lib/node-logger.js` 没有任何引用**：要么让 `compress-images.js` 等构建端脚本用起来（第 8.2 节），要么删除。
- [ ] **`package.json` 残留 `@renbaoshuo/markdown-it-katex`**：没有被启用，与第 5.3 节「不要再装 katex 相关插件」冲突，建议 `npm uninstall`。
- [ ] **`README.md` 是 UTF-16 LE 编码**（多半是 PowerShell 重定向写出来的）：GitHub 能显示，但很多工具会把它当二进制或显示乱码，建议另存为 UTF-8。
- [ ] **`scaffolds/post.md` 缺 `categories`**：新建文章默认没有分类，`npm test` 会警告（当前 `博客制作相关/我的第一篇博客.md` 就缺）。建议给模板加上 `categories:`，并给这篇补分类。
- [ ] **`test/validate-build.js` 把 `/azur_blog/` 写死**（第 1、4、5 项），与它自己开头「不应再写死」的注释矛盾；且「版本号缺失」「关键资源缺失」只是警告、不阻止部署（第 1.1 节）。是否要改成错误，取决于你希望校验多严。
- [ ] **画廊性能**：`gallery.js` 一次性渲染全部图片，且每张图都读一次 `offsetHeight`（强制回流）；`gallery.json` 里只有原图地址，缩略图就是原图；`scripts/gallery-list.js` 逐张串行读取尺寸。已记录为 `需要解决的问题.md` 的 P2-10 / P2-11，图库已 1788 张，建议近期处理。
- [ ] **主题管理方式**：`themes/butterfly/` 以 vendor 方式在 git 里，且被深度定制（第 7 节列了 7 个被改文件），升级需谨慎。
- [ ] **画廊图片体积**：`source/carousel/` 当前 1788 张，含 1MB+ 大图（最大约 1.4MB），建议定期跑 `node compress-images.js`。（未验证：图只在本机，仓库里没有它们）
- [ ] **daily-log.js 的「改动文件」归因偶尔错位一格**：见第 8.2 节末尾的已知小瑕疵，不影响构建本身，暂不紧急。
- [ ] **markdown-it 全站排版行为切换的副作用未逐篇核实**：`breaks: true`（单个换行变 `<br>`）、`typographer: true`（自动转换引号/破折号等排版符号）是切换渲染器时顺带打开的全局配置，只验证过差分隐私第六章笔记这一篇渲染正常，其余文章尚未逐一核对排版有没有跟着变样，建议抽查几篇换行习惯随意的旧文章。
- [ ] **在手机上加载时网站封面上的字有些不明显**（`welcome_animation.css` 里 `.welcome-title` 的颜色透明度只有 0.4，且标题动画大半时间处于被裁切/淡出状态）。

说明：CI 已删除（有意）——画廊图只在本机，云端构建会空画廊；部署走本地 `npm run deploy` 或 `auto-deploy.cmd`（见第 11 节）。`.github/dependabot.yml` **不是**死代码，它独立于 Actions 工作，仍每日扫描 npm 依赖提 PR。

---

## 14. 文档与源码的同步规则

本文档的目标是**与源码严格对应**。做到这一点靠两条：

1. **改了下面左列的东西，就同步改右列的章节**（同一次提交里改，不要留到以后）：

| 你改了 | 同步改本文的 |
|---|---|
| `package.json` 的 scripts | 第 1 节命令表 |
| `test/validate-build.js`（增删检查、改级别） | 第 1.1 节、第 10 节 |
| `source/js/` 或 `source/css/` 里增删文件，或改 `inject:` 的顺序 | 第 2 节目录树、第 3 节、第 5.1 / 5.2 节（含加载顺序） |
| 某个模块是否接入 `BlogLifecycle` | 第 5.1 节的「迁移状态」、第 9 节的名单 |
| `scripts/` 里增删文件 | 第 2 节、第 10 节的清单 |
| `auto-deploy.cmd` 的步骤、询问、汇总状态 | 第 11 节 |
| `settings-menu.js` / `settings-menu.css` 的键名、属性、数值范围 | 第 5.4 节 |
| 主题 `_config.yml` 的 `inject` / `pjax` / 外部 CDN | 第 3、5、9、12 节 |
| 改了 `themes/butterfly/` 下的任何文件 | 第 7 节的「被改文件」表 |
| 修完第 13 节的某项 | 把它挪到「已解决」，写清修复方式 |

2. **用脚本检查**：运行 `node test/check-docs.js`。它会核对「文档里写到的路径与命令是否真实存在」「`package.json` 的每个脚本是否都在命令表里」「`source/js` 的每个文件是否都在第 5.1 节、`source/css` 的每个文件是否都在第 5.2 节、`scripts` / `test` / `lib` 的每个文件是否都在第 2 节目录树里」「`inject` 里的本地资源是否都在文档里出现」「文中的『第 X 节』是否指向存在的章节」。它能发现「文件删了、文档还写着」和「新增了文件、文档没提」，但**发现不了「行为变了、描述没变」**——那一类只能靠上面第 1 条的自觉。
