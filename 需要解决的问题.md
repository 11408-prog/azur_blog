项目技术债与待优化问题

本文用于记录当前 azur_blog 项目中已经发现的技术问题、潜在风险以及后续优化方向。

> **维护约定（重要）**：这份清单只在"发现问题时"更新，修复后**不会自动同步**。
> 处理某一条之前，先去对应源文件搜索本文档编号（如 `P1-6`）确认是否已经修过——
> 代码里对应的修复通常直接在注释里写了"见《需要解决的问题.md》P1-x"。
> 确认修复后请把该条目移到下方"已解决"区并简述修复方式，**不要直接删除**，
> 也不要凭旧记录重新"修一遍"（可能会把已经写好的方案改回旧版本）。
> 最近一次全面核对：2026-09-18（对照 `main` 分支源码逐条核实）。

优先级：
P0：应优先处理，可能影响功能正确性或造成明显性能/维护问题
P1：建议近期处理，属于架构或维护性问题
P2：项目规模扩大后再处理，当前不影响核心功能

---

## 一、已解决（历史记录，勿再改回）

### ✅ P0：日志系统重复写入 Buffer
涉及文件：`source/js/logger.js`

原问题：`debug()`/`info()` 会调用 `pushBuffer()`，Debug 模式下又会经 `out()` 再调用一次 `pushBuffer()`，导致同一条日志写入两次；全局 `error`/`unhandledrejection` 处理器有同样的问题。

修复方式：统一成唯一入口 `out() → pushBuffer()`，调用方（`debug`/`info`/`warn`/`error`/全局错误捕获）都只调用 `out()`，不再各自直接调 `pushBuffer()`。

### ✅ P0：日志系统频繁同步写入 localStorage
涉及文件：`source/js/logger.js`

原问题：`pushBuffer()` 每执行一次就同步 `localStorage.setItem()`，日志密集时（PJAX/图片加载/异常等）造成较多同步 I/O。

修复方式：内存 Buffer 与持久化分离，加了 `_dirty`/`_pendingCount` 标记 + `FLUSH_THRESHOLD`（20 条）/`FLUSH_INTERVAL`（5 秒）节流写入，并在 `pagehide`/`visibilitychange` 时兜底 flush，保证页面离开前落盘。

### ✅ P1：PJAX 生命周期管理不统一
涉及范围：`source/js/*.js`

原问题：多个文件各自用 `DOMContentLoaded` + `pjax:complete` 初始化，定时器/监听器/DOM 节点缺少统一清理机制，容易造成重复实例、内存泄漏。

修复方式：落地 `source/js/lifecycle.js`（`BlogLifecycle`），提供 `register(name, {mount, destroy, persistent})`，`carousel.js`/`gallery.js`/`activity.js`/`video-cover.js`/`welcome_animation.js`/`music.js`/`weather.js`/`toc-check.js` 全部已接入，全站只剩一个 `pjax:complete` 监听入口。`random_posts.js` 本身不涉及定时器/监听器，未接入不影响功能。详见《开发守则.md》第 9 节。

### ✅ P1：音乐播放器 PJAX 生命周期需要重构
涉及文件：`source/js/music.js`

原问题：若每次 PJAX 都重新初始化 APlayer，可能造成播放器重复实例、音频状态丢失。

修复方式：接入 `BlogLifecycle` 且 `persistent: true`，全局只创建一次播放器实例，PJAX 后只做健康检查 + UI 同步，不重建。

### ✅ P1：天气模块 PJAX 下可能重复请求
涉及文件：`source/js/weather.js`

原问题：`DOMContentLoaded` 与 `pjax:complete` 都会触发初始化，即使有缓存也会产生不必要的初始化开销。

修复方式：数据层（`fetch` + 30 分钟内存缓存 + `_fetching` 并发保护）与 UI 层彻底分离；脚本挂载在 `#body-wrap` 之外、整页只执行一次，PJAX 后只用缓存重绘 UI，不重新拉数据。

### ✅ P1：activity.js 存在硬编码日期
涉及文件：`source/js/activity.js`

原问题：热力图统计起始日期硬编码在前端 JS 里，项目推进后不会自动更新。

修复方式：改为从 `data/activity.json`（`scripts/activity.js` 构建期生成）里取实际最早的一天作为统计起点，不再硬编码具体日期。

### ✅ P1：置顶文章依赖标题字符串判断
涉及文件：`source/js/random_posts.js`

原问题：置顶逻辑按标题精确匹配 `PINNED` 数组，改标题会导致置顶失效；且与主题原生 `sticky` 字段形成两套并存机制。

修复方式：改为读取构建期渲染的 front matter 标记（`article.top || article.sticky > 0` → `indexPostUI.pug` 输出 `i.sticky`），运行期只检测该标记，两套机制合并为一套。文章标题现在可以自由修改，不影响置顶。

### ✅ P1：项目路径存在硬编码风险
涉及范围：`source/js/`、`scripts/`

原问题：多个文件直接写死 `/azur_blog/` 前缀，部署路径一变就大范围失效。

修复方式：新增 `source/js/blog-config.js`（`BlogConfig`），从 Butterfly 输出的 `GLOBAL_CONFIG.root` 派生统一路径，提供 `BlogConfig.url(p)`；全站脚本改为从这里取路径，只在该文件内保留一处兜底字面量。

### ✅ P2：前端模块初始化方式过于分散（基本解决）
涉及范围：`source/js/*.js`

原问题：各模块各自维护 `DOMContentLoaded`/`pjax:complete`/定时器/Observer，长期会导致生命周期不一致、难以定位问题。

现状：随着 P1-3（PJAX 生命周期）落地，绝大部分模块已统一走 `BlogLifecycle`，全站只有一个监听入口，问题描述里担心的"分散"情况已基本消除。`random_posts.js` 未接入，但它不持有需要清理的资源，不构成风险。

### ✅ P2：需要减少不必要的前端 Debug 日志
涉及范围：`source/js/`

原问题：担心生产环境残留大量 `console.log` 造成噪声和信息泄露。

现状：`DSLog`（`logger.js`）从设计上就把 `debug`/`info` 默认隐藏（只有 `warn`/`error` 始终打印到控制台），且做了路径用户名和长 token 的脱敏处理（见 `sanitize()`）。业务代码统一走 `DSLog.*`，未见散落的裸 `console.log`。该问题从架构层面已经满足验收标准，不需要额外改动。

---

## 二、仍待处理

### P1：scripts 目录应该被视为构建系统的一部分（流程未完全落实）
涉及目录：`scripts/`（`activity.js`、`carousel.js`、`daily-log.js`、`gallery-list.js`）

问题描述：这些脚本参与 Hexo 构建期的数据生成，属于构建系统的一部分。修改后如果没有跑完整构建测试，可能出现"JS 不报错，但生成的数据结构变了，前端悄悄异常"的情况。

现状：《开发守则.md》已经把 `npm run test:build` 作为日常推荐流程（第 1.1 节），但**没有专门针对"修改 scripts/ 后必须跑 test:build"写一条强制规则**——目前只有"新增 `source/js/` 文件后必须跑 test:build"这一条是明确写出来的（第 9 节末尾踩坑记录）。

建议：在《开发守则.md》第 10 节补一条："修改 `scripts/` 下任意文件后，必须跑一次 `npm run test:build`，确认生成的 JSON 结构仍符合 `test/validate-build.js` 的 10 项校验"。这是纯流程约定，不涉及代码改动，可以直接采纳。

### P2：Gallery 瀑布流存在重复布局计算
涉及文件：`source/js/gallery.js`

问题描述：通过读取 `offsetHeight` 寻找最短列，属于可能触发 Layout/Reflow 的 DOM 属性；图片数量非常大时反复读取可能造成较多重排。

现状：当前已使用图片尺寸占位策略（`w`/`h` 做 `aspect-ratio`），减少了图片加载导致的布局跳动，问题不紧急。

建议：图库规模扩大后，可以在 JS 里维护一份列高度缓存（如 `columnHeights = [1200, 950, ...]`），新增图片时增量更新，而不是每次重新读 DOM。

优先级：P2，当前无需为理论性能过早重构。

### P2：gallery-list.js 图片元数据读取可以并行化
涉及文件：`scripts/gallery-list.js`

问题描述：当前用 `for...of` + `await sharp(...).metadata()` **串行**读取每张图的尺寸（已核实，截至 2026-09-18 仍是串行实现）。图库图片数量少时影响很小，数量涨到数百/上千张后构建速度会明显下降。当前 `source/carousel/` 已有 1788 张图（见《开发守则.md》第 13 节），已经进入"数量较多"区间，值得提上日程。

建议：改成 `Promise.all()` 或受控并发（如同时处理 4～8 张），不要无限制并发。示例思路：

```js
// 分批并发，避免一次性打开过多文件句柄
const CONCURRENCY = 6;
for (let i = 0; i < files.length; i += CONCURRENCY) {
  const batch = files.slice(i, i + CONCURRENCY);
  const results = await Promise.all(batch.map(async (f) => { /* 读取单张 metadata */ }));
  images.push(...results);
}
```

优先级：P2，但鉴于图库规模已经不小，建议纳入近期处理。

### P2：大型图片资源需要持续关注加载性能
涉及范围：`source/carousel/`、`source/img/`

问题描述：随着 Gallery、轮播、背景图等视觉资源持续增加，页面资源体积和移动端加载时间会随之上升。这是一个**持续性关注事项**，不是一次性能修完的 bug。

现状：项目已有图片尺寸检测（`gallery-list.js` 写入 `w`/`h`）和压缩脚本（`compress-images.js`），不建议重复建设。

建议：后续按需推进 WebP/AVIF、lazy loading、首屏优先加载等，但不必现在动手，等实际观察到加载变慢再处理。

---

## 三、技术债现状总表

| 编号 | 问题 | 状态 |
|---|---|---|
| P0-1 | Logger 重复写入 Buffer | ✅ 已解决 |
| P0-2 | Logger 频繁同步写 localStorage | ✅ 已解决 |
| P1-3 | PJAX 生命周期不统一 | ✅ 已解决 |
| P1-4 | Music PJAX 重复初始化风险 | ✅ 已解决 |
| P1-5 | Weather PJAX 重复初始化/请求 | ✅ 已解决 |
| P1-6 | Activity 日期硬编码 | ✅ 已解决 |
| P1-7 | Random Posts 依赖标题判断置顶 | ✅ 已解决 |
| P1-8 | `/azur_blog/` 路径硬编码 | ✅ 已解决 |
| P1-9 | 构建脚本缺少明确维护规范 | ⏳ 待处理（流程未写死） |
| P2-10 | Gallery 重复 Layout 计算 | ⏳ 待处理（不紧急） |
| P2-11 | Gallery metadata 串行读取 | ⏳ 待处理（图库已 1788 张，建议近期处理） |
| P2-12 | JS 模块初始化过于分散 | ✅ 基本已解决 |
| P2-13 | 生产环境 Debug 日志过多 | ✅ 已解决（架构层面满足） |
| P2-14 | 图片资源持续增长 | 🔁 持续关注（非一次性修复项） |

---

## 四、修改原则（保留，长期有效）

### 4.1 不要为了优化而破坏现有功能

所有优化必须满足优先级：**功能正确性 > 架构优化 > 性能优化 > 代码美观**。不要为了减少几行代码而改变已经稳定工作的功能。

### 4.2 不要一次性大规模重构

推荐：一个模块 → 修改 → 测试 → 确认 → 再修改下一个模块。

### 4.3 修改前先阅读现有实现

AI 修改项目代码前，应先：阅读目标文件 → 阅读相关配置 → 阅读调用方 → 确认生命周期 → 再修改。禁止看到一个问题就直接重写整个文件。

### 4.4 优先解决真实问题

不要为了所谓的"最佳实践"引入复杂架构。优化应当建立在实际问题、实际收益、项目规模之上。

---

## 五、最终目标

当前项目已经从"简单 Hexo 博客"发展为 Hexo + Butterfly + 自定义 JS/CSS + 构建脚本 + 数据生成 + PJAX + 第三方组件的组合体。截至本次核对（2026-09-18），列表里绝大多数架构性问题（日志、PJAX 生命周期、路径硬编码、置顶机制）已经解决，剩下的都是 P1/P2 级别的流程规范和性能优化项，不影响核心功能正确性。

后续维护重点应继续保持在：稳定性 + 生命周期管理 + 性能 + 可维护性 + 自动化测试，核心目标不是让代码"看起来更复杂"，而是让项目在继续增加功能时不会越来越乱。
