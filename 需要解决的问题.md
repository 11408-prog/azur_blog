项目技术债与待优化问题

本文用于记录当前 azur_blog 项目中已经发现的技术问题、潜在风险以及后续优化方向。

优先级：
P0：应优先处理，可能影响功能正确性或造成明显性能/维护问题
P1：建议近期处理，属于架构或维护性问题
P2：项目规模扩大后再处理，当前不影响核心功能

一、P0：日志系统存在重复写入问题

涉及文件：
source/js/logger.js

问题描述：

当前日志系统存在重复调用 pushBuffer() 的情况。

debug() 和 info() 本身已经调用 pushBuffer()。

当 Debug 模式开启后，又会继续调用 out()，而 out() 内部同样会调用 pushBuffer()。

因此同一条日志可能被写入 Buffer 两次。

全局 error 和 unhandledrejection 处理器中也存在类似问题：

pushBuffer()
→ out()
→ pushBuffer()

影响：

1. 日志 Buffer 中出现重复记录
2. localStorage 写入次数增加
3. Debug 模式下问题更加明显
4. 长期运行时会增加不必要的存储和序列化开销

建议：

统一日志写入路径。

推荐：

debug/info/warn/error
↓
out()
↓
pushBuffer()
↓
localStorage

不要让调用方和 out() 同时负责 Buffer 写入。

验收标准：

1. 一条日志只进入 Buffer 一次
2. Debug 模式不会产生重复日志
3. 全局 error / unhandledrejection 不重复写入
4. 原有 Console 输出行为保持不变

二、P0：日志系统频繁同步写入 localStorage

涉及文件：

source/js/logger.js

问题描述：

当前 pushBuffer() 每执行一次都会进行 localStorage.setItem()。

也就是说，每产生一条日志，都可能进行：

数组截取
↓
JSON.stringify
↓
localStorage.setItem

localStorage 是同步 API。

影响：

如果页面存在大量日志，例如：

PJAX
图片加载
网络请求
JavaScript 异常
Debug 日志

可能造成较多同步 I/O。

建议：

将“内存 Buffer”和“localStorage 持久化”分离。

例如：

日志产生
↓
写入内存 Buffer
↓
延迟/批量写入 localStorage

或者仅在以下情况下进行持久化：

1. 页面离开
2. 日志数量达到阈值
3. 定时器触发

注意：

不要为了优化而彻底删除日志持久化功能。

三、P1：PJAX 生命周期管理不统一

涉及范围：

source/js/
尤其包括：

source/js/music.js
source/js/weather.js
source/js/toc-check.js

问题描述：

当前多个 JS 文件采用类似：

DOMContentLoaded

以及：

pjax:complete

的方式初始化。

随着功能增加，可能出现：

DOMContentLoaded
↓
初始化

PJAX
↓
再次初始化

再次 PJAX
↓
再次初始化

如果初始化函数内部创建：

1. EventListener
2. setInterval
3. setTimeout
4. MutationObserver
5. ResizeObserver
6. DOM 节点
7. 第三方实例

而没有对应清理机制，就可能产生重复实例。

影响：

1. 同一个事件触发多次
2. 定时器重复运行
3. 网络请求重复发送
4. DOM 元素重复创建
5. 音乐播放器重复初始化
6. 内存泄漏
7. PJAX 页面运行时间越长越不稳定

建议：

建立统一的 PJAX 生命周期规范。

推荐：

init()
↓
mount()
↓
页面运行
↓
destroy()

对于不需要销毁的全局模块，则保证 init() 必须幂等。

即：

初始化一次
重复调用不会产生副作用。

四、P1：音乐播放器 PJAX 生命周期需要重构

涉及文件：

source/js/music.js

问题描述：

当前音乐播放器初始化逻辑存在关于 PJAX 重初始化的技术债。

音乐播放器属于全局持久化组件，而普通页面内容属于 PJAX 页面生命周期。

两者生命周期并不相同。

如果每次 PJAX 页面切换都重新初始化 APlayer，可能造成：

1. 播放器重复实例
2. 音频状态丢失
3. EventListener 重复
4. 播放状态异常
5. DOM 重复

建议：

将音乐播放器拆分为：

全局播放器实例
↓
只初始化一次

PJAX 页面切换
↓
仅更新必要的页面相关内容

不要让 PJAX 页面切换重新创建播放器实例。

验收标准：

连续进行：

进入页面
→ PJAX 跳转
→ PJAX 跳转
→ 返回
→ 再跳转

播放器应该：

1. 只存在一个实例
2. 不重复播放
3. 播放状态正常
4. 不出现重复事件
5. 不产生重复 DOM

五、P1：天气模块 PJAX 下可能重复请求

涉及文件：

source/js/weather.js

问题描述：

当前天气模块在 DOMContentLoaded 和 pjax:complete 等生命周期中调用初始化逻辑。

虽然已经存在缓存以及 30 分钟相关机制，但 PJAX 页面切换仍可能重复执行初始化。

影响：

可能出现：

PJAX
↓
initWeather()
↓
检查/请求天气

反复执行。

即使最终因为缓存避免了真正的网络请求，也会产生不必要的初始化开销。

建议：

将天气数据缓存与 UI 初始化分离：

天气数据层
↓
统一缓存
↓
页面 UI

PJAX 时：

优先读取缓存
↓
直接更新 UI

而不是重新执行完整的数据获取流程。

验收标准：

连续 PJAX：

A → B → C → D

不会因为页面切换而重复进行不必要的天气 API 请求。

六、P1：activity.js 存在硬编码日期

涉及文件：

source/js/activity.js

问题描述：

当前活动热力图相关逻辑中存在类似：

2026-08-11

这样的固定日期。

这意味着项目时间继续推进后，代码中的日期不会自动更新。

影响：

1. 活跃时间范围显示过期
2. 新文章无法正确反映到时间范围
3. 后续维护时需要手动修改 JS
4. 项目长期运行后出现数据与显示不一致

建议：

不要将当前日期直接硬编码在前端 JS 中。

可以考虑：

Git commit 日期

或者：

生成 activity.json 时自动确定时间范围。

推荐结构：

Git / 文章数据
↓
scripts/activity.js
↓
data/activity.json
↓
前端读取

让数据生成负责确定日期范围，而不是前端 JS 自己维护日期。

七、P1：置顶文章依赖标题字符串判断

涉及文件：

source/js/random_posts.js

问题描述：

当前随机文章/置顶文章相关逻辑通过文章标题进行精确匹配。

标题属于内容，不应该承担程序逻辑标识符的职责。

如果修改文章标题：

旧标题
↓
新标题

可能导致置顶逻辑失效。

建议：

使用 Hexo Front Matter。

例如：

---

title: 文章标题
top: true
---------

前端根据 top 进行判断。

也可以进一步使用：

sticky: true

等统一字段。

优点：

标题可以自由修改
↓
置顶状态不会受到影响

八、P1：项目路径存在硬编码风险

涉及范围：

source/js/
scripts/

问题描述：

部分代码直接使用：

/azur_blog/

作为资源路径。

例如：

/azur_blog/carousel/

风险：

如果以后修改部署方式：

GitHub Pages
↓
自定义域名

或者：

/azur_blog/
↓
/

大量硬编码路径都可能失效。

建议：

优先使用 Hexo：

root: /azur_blog/

提供的根路径机制。

或者建立统一的前端路径变量。

原则：

不要在大量 JS 文件中分别维护：

/azur_blog/

而应该：

统一配置
↓
统一生成
↓
统一引用

验收标准：

项目从：

/azur_blog/

切换到：

/

时，不需要大量修改 JS。

九、P1：scripts 目录应该被视为构建系统的一部分

涉及目录：

scripts/

包括：

activity.js
carousel.js
daily-log.js
gallery-list.js

问题描述：

这些脚本并不是普通的辅助脚本，而是已经参与：

Hexo 构建
↓
数据生成
↓
页面展示

因此属于项目构建系统的一部分。

风险：

如果 AI 或开发者修改这些脚本后没有执行完整构建测试，可能出现：

JS 没报错
↓
但是生成的数据结构发生变化
↓
前端页面异常

建议：

修改 scripts/ 中的任何文件后，至少执行：

npm run test:build

原则：

构建脚本修改必须经过：

修改
↓
构建
↓
自动测试
↓
页面验证

十、P2：Gallery 瀑布流存在重复布局计算

涉及文件：

source/js/gallery.js

问题描述：

当前瀑布流通过读取：

offsetHeight

来寻找最短列。

这属于可能触发布局计算的 DOM 属性。

如果图片数量非常大，反复读取 offsetHeight 可能造成较多 Layout/Reflow。

当前评价：

当前实现已经使用图片尺寸占位策略，这一点是正确的，可以降低图片加载导致的布局跳动。

因此这个问题目前不是紧急问题。

建议：

当图库规模扩大后，可以考虑维护：

JS 中的列高度缓存

而不是每次重新读取 DOM。

例如：

columnHeights = [
1200,
950,
1100,
870
]

新增图片后直接更新对应列高度。

优先级：

P2

目前无需为了理论性能而过早重构。

十一、P2：gallery-list.js 图片元数据读取可以并行化

涉及文件：

scripts/gallery-list.js

问题描述：

当前使用 sharp 获取图片尺寸时，基本采用：

一张图片
↓
读取 metadata
↓
下一张图片
↓
读取 metadata

属于串行处理。

影响：

当图库图片数量较少时影响很小。

当图片数量增长到数百张甚至上千张时，构建速度可能明显下降。

建议：

可以改成：

Promise.all()

或者：

受控并发

例如同时处理 4～8 张。

注意：

不要简单地无限制：

Promise.all(所有图片)

如果图库非常大，应该使用并发限制。

优先级：

P2

十二、P2：前端模块初始化方式过于分散

涉及范围：

source/js/*.js

问题描述：

当前多个 JS 文件各自管理：

DOMContentLoaded
pjax:complete
定时器
Observer
事件监听器

这种方式在项目规模较小时简单直观。

但随着功能增加，会逐渐出现：

模块 A
↓
自己的 PJAX 监听

模块 B
↓
自己的 PJAX 监听

模块 C
↓
自己的 PJAX 监听

模块 D
↓
自己的 PJAX 监听

长期风险：

初始化逻辑分散
↓
生命周期不一致
↓
重复初始化
↓
难以定位问题

建议：

后续逐步建立统一模块规范。

例如：

BlogModules = {
music: {
init() {},
destroy() {}
},

```
weather: {
    init() {},
    destroy() {}
},

gallery: {
    init() {},
    destroy() {}
}
```

}

然后统一处理：

页面首次加载
PJAX 页面切换
页面销毁

注意：

不要一次性重构所有 JS。

应该：

出现问题的模块
↓
逐个迁移

十三、P2：需要减少不必要的前端 Debug 日志

涉及范围：

source/js/

问题描述：

开发阶段大量日志非常有价值。

但生产环境长期保留大量：

console.log()
console.debug()

会造成：

1. Console 噪声
2. 日志系统额外工作
3. 调试信息泄露
4. 不利于定位真正异常

建议：

区分：

开发模式

和：

生产模式

例如：

开发环境：
详细 Debug 日志

生产环境：
只保留 warn/error

或者通过：

DSLog.debug()

统一控制。

原则：

不要在业务 JS 中到处直接使用：

console.log()

统一通过：

DSLog

管理。

十四、P2：大型图片资源需要持续关注加载性能

涉及范围：

source/carousel/
source/img/

问题描述：

博客已经存在：

Gallery
轮播
背景图
Enterprise 图片

等大量视觉资源。

随着图库继续增加，可能出现：

图片数量增加
↓
页面资源体积增加
↓
首次加载时间增加
↓
移动端性能下降

建议：

后续逐步完善：

1. 图片尺寸控制
2. WebP / AVIF
3. lazy loading
4. 首屏图片优先加载
5. 非首屏图片延迟加载
6. 图片压缩
7. 合理的缩略图策略

注意：

目前项目已经有图片尺寸检测和相关构建脚本，不建议为了优化而重复建设已有功能。

十五、技术债优先级总表

P0：

1. Logger 重复写入 Buffer
   文件：source/js/logger.js
   建议：优先修复

2. Logger 频繁同步写 localStorage
   文件：source/js/logger.js
   建议：批量/延迟持久化

P1：

3. PJAX 生命周期不统一
   文件：source/js/*.js
   建议：建立 init/destroy 规范

4. Music PJAX 重复初始化风险
   文件：source/js/music.js
   建议：全局播放器与页面生命周期分离

5. Weather PJAX 重复初始化/请求
   文件：source/js/weather.js
   建议：数据缓存与 UI 分离

6. Activity 日期硬编码
   文件：source/js/activity.js
   建议：从生成数据自动获取

7. Random Posts 依赖标题判断置顶
   文件：source/js/random_posts.js
   建议：使用 Front Matter

8. /azur_blog/ 路径硬编码
   文件：JS / Scripts
   建议：统一路径配置

9. 构建脚本缺少明确维护规范
   文件：scripts/
   建议：修改后执行完整构建测试

P2：

10. Gallery 重复 Layout 计算
    文件：source/js/gallery.js
    建议：大规模图库后优化

11. Gallery metadata 串行读取
    文件：scripts/gallery-list.js
    建议：受控并发

12. JS 模块初始化过于分散
    文件：source/js/*.js
    建议：逐步模块化

13. 生产环境 Debug 日志过多
    文件：source/js/*.js
    建议：统一日志控制

14. 图片资源持续增长
    文件：source/carousel/ 等
    建议：持续优化图片加载

十六、推荐处理顺序

不要一次性全部重构。

推荐按照以下顺序处理：

第一阶段：

Logger 重复 Buffer
↓
Logger localStorage 写入

第二阶段：

PJAX 生命周期规范
↓
Music 生命周期
↓
Weather 缓存

第三阶段：

Activity 日期
↓
Random Posts 置顶机制
↓
路径统一

第四阶段：

构建脚本规范
↓
测试规范
↓
性能检查

第五阶段：

Gallery 性能
↓
metadata 并发
↓
JS 模块化

十七、修改原则

17.1 不要为了优化而破坏现有功能

所有优化必须满足：

功能正确性

>

架构优化

>

性能优化

>

代码美观

不要为了减少几行代码而改变已经稳定工作的功能。

17.2 不要一次性大规模重构

尤其不要同时修改：

Butterfly
+
PJAX
+
Music
+
Weather
+
Logger
+
Gallery

推荐：

一个模块
↓
修改
↓
测试
↓
确认
↓
再修改下一个模块

17.3 修改前先阅读现有实现

AI 修改项目代码前，应先：

阅读目标文件
↓
阅读相关配置
↓
阅读调用方
↓
确认生命周期
↓
再修改

禁止：

看到一个问题
↓
直接重写整个文件

17.4 优先解决真实问题

不要为了所谓的：

最佳实践

而引入复杂架构。

优化应当建立在：

实际问题
+
实际收益
+
项目规模

之上。

十八、最终目标

当前项目已经从：

简单 Hexo 博客

逐渐发展为：

Hexo
+
Butterfly
+
自定义 JS
+
自定义 CSS
+
构建脚本
+
数据生成
+
PJAX
+
第三方组件

因此后续维护重点应逐渐从：

添加新功能

转向：

稳定性
+
生命周期管理
+
性能
+
可维护性
+
自动化测试

核心目标不是让代码“看起来更复杂”，而是让项目在未来继续增加功能时：

不会越来越乱
不会因为 PJAX 出现重复初始化
不会因为修改一个模块影响其他模块
不会因为路径变化导致资源全部失效
不会因为构建脚本修改导致页面静默损坏

最终形成：

稳定功能
+
清晰架构
+
可控生命周期
+
自动化构建
+
自动化测试
+
明确技术债

作为 azur_blog 后续长期维护的基础。
